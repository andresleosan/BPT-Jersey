# tasksv2.md - BPT Jersey Academy Platform, segunda version

Estados: `pendiente` -> `en-progreso` -> `revision` -> `aprobada` -> `desplegada`; `bloqueada` cuando
requiere una decision o un dato externo que todavia no existe.

Cada tarea con impacto en codigo pasa el mismo ciclo de autocritica Nivel 3 que `tasks.md`:
seguridad, pruebas relevantes, evidencia y rendimiento cuando corresponda.

## Que es este documento y que no es

Este ledger cubre **unicamente** los bugs y las funciones nuevas levantados por el operador el
2026-09-09. `tasks.md` sigue siendo la fuente de verdad de todo lo anterior, incluidas las filas que
siguen abiertas alli (T010, T011, T125, T126, T127). Nada de lo que hay aqui las sustituye ni las
cierra.

Las filas se numeran `T001V2` en adelante. El sufijo `V2` es lo que mantiene los IDs unicos a lo
largo de los dos ledgers: `T001V2` no es `T001`, y ningun ID de este archivo puede confundirse con
uno de `tasks.md` aunque compartan el numero.

## Regla de honestidad de este ledger

Dos requisitos del operador resultaron estar ya construidos al auditar el codigo antes de escribir
estas filas. No se convierten en tareas de construccion, sino de verificacion, y la fila lo dice:
inventar trabajo ya hecho seria mentir sobre el estado del proyecto. Son T005V2 (el texto del waiver)
y T020V2 (el centro de entrenamiento en la base de miembros).

## Decisiones de producto tomadas por el operador el 2026-09-09

| # | Decision | Consecuencia directa |
| - | -------- | -------------------- |
| D1 | El menor tiene cuenta propia con alcance recortado | Ve su progreso y sus proximas clases, reserva y marca asistencia. No ve pagos, waiver ni datos de terceros. El representante legal sigue siendo titular de suscripcion, waiver y responsabilidad, y puede revocar el acceso. Entra en la DPIA de T011 como tratamiento nuevo. |
| D2 | Cancelar con menos de 12 h reutiliza el fee de inasistencia existente | No se crea un cargo nuevo: se emite la misma **propuesta** de penalizacion de T111 (GBP 15, Town) que un administrador aprueba o descarta. Nunca hay cobro automatico. |
| D3 | La primera clase se reserva con cuenta creada, antes de la aprobacion | El solicitante se registra, reserva su clase gratis y eso dispara la notificacion al admin. Queda en estado pendiente: ve su reserva, pero no puede reservar una segunda clase hasta ser aprobado. |
| D4 | v2 cubre solo lo nuevo | `tasks.md` y `Lista/` conservan la verdad de todo lo anterior. |
| D5 | La cola de aprobacion se opera desde la cuenta de `owner` | No se escribe a mano el documento de personal que le falta a la cuenta de `administrator`: la de `owner` ya pasa la puerta canonica y aprueba hoy, asi que desbloquear T012V2 cuesta cero escrituras en produccion. La cuenta de `administrator` queda inservible para el directorio canonico **a proposito**, y eso se declara en T012V2 como limitacion conocida, no como un olvido. |
| D6 | `provisionAdminRole` se despliega como callable, en fila propia | Conceder un rol administrativo deja de ser una operacion de consola. Abre T022V2: toca la superficie de autorizacion, asi que lleva su propia revision y sus propias pruebas y no cuelga de T012V2. Es lo que levanta despues la limitacion que D5 acepta hoy. |
| D7 | `members` sale de la precondicion de vacuidad de la inicializacion | Medido en produccion el 2026-09-09: `academies/demo-academy/members` tiene **243 registros reales** importados del PDF el 2026-08-13 (`source: "member-pdf-import"`, evento `member.import.confirmed`), asi que el boton habria devuelto `failed-precondition` nombrando esa coleccion. `members` no es el directorio canonico -que vive en `students`, `studentIdentityKeys` y `studentAdminProfiles`, las tres vacias- sino la **coleccion origen** de la migracion forward, y esa migracion exige `identityKeyCoverage: "complete"` con linea base (`member-directory-forward-runner.ts:142-156`) y el documento de estado (`member-directory-forward-runner-firestore.ts:114`): justo lo que solo el inicializador crea. Con `members` en la lista no se puede inicializar ni migrar, y los 243 se quedan sin camino para siempre. Sale por el mismo razonamiento que saco a `auditEvents`. Consecuencia aceptada a sabiendas: tras inicializar, `/admin/members` muestra **cero miembros** hasta que se migren los 243, y hoy no hay ninguna callable de migracion desplegada. |
| D8 | El claim de `admin@admin.com` sube a `owner`, a sabiendas de que no le abre el directorio | Pedido por el operador el 2026-09-09 y ejecutado tras exponerle la limitacion. **Este cambio no pasa por `provisionAdminRole`, asi que no emite `admin.role.granted`: esta fila es el unico rastro de quien lo concedio.** El claim de `academies/demo-academy` pasa de `{"academyId":"demo-academy","role":"administrator"}` a `{"academyId":"demo-academy","role":"owner"}` en el uid `BIuILHd7xnT3FwPwKrtBH21SnU63`; para deshacerlo basta reponer el valor anterior, que queda escrito aqui entero a proposito. **Lo que compra y lo que no:** el shell dira `OWNER ACCESS` y se abren las pantallas que solo miran el claim, pero **el directorio canonico le seguira devolviendo 403**, porque la cuenta es de contrasena y su puerta exige personal de Google. El claim solo surte efecto tras renovar el token, es decir, cerrando sesion y volviendo a entrar. |
| D9 | El acceso administrativo se queda en las dos cuentas que ya existen | Pedido por el operador el 2026-09-09 -"deja solo andres y admin"- despues de que la lectura de produccion mostrara que la cuenta que habia nombrado es el estudiante que dio de alta T012V2. **No se asciende ninguna cuenta cliente.** Lo que se descarta a sabiendas: limpiar a mano el claim `adultStudent` y el documento cliente para que la callable aceptara el objetivo. Costaba el alta -el unico documento de `students` apunta a ese uid y quedaria huerfano- y no habia forma auditada de hacerlo: no existe ninguna via de baja de miembro en `apps/functions` ni en `packages/domain`, y `provisionAdminRole` solo revoca roles administrativos. **Consecuencia aceptada, y es la que importa:** no hay un segundo owner. La limitacion que D5 declaro sigue en pie sin levantar, porque `admin@admin.com` conserva el claim que le dio D8 y sigue sin poder abrir el directorio canonico. **En la practica el trabajo administrativo real depende de una sola cuenta**, la de `owner` de Google, y eso no lo arregla T022V2: necesita otra cuenta de Google o una decision de producto sobre las cuentas de contrasena. |

---

## V2-A - Bugs de interfaz del formulario de inscripcion

| ID   | Tarea atomica | Depende de | Estado | Evidencia de salida |
| ---- | ------------- | ---------- | ------ | ------------------- |
| T001V2 | Permitir vaciar por completo Nombre y Email en el formulario de inscripcion | - | desplegada | Causa localizada antes de abrir la fila: en `apps/web/src/app/enrol/page.tsx:378-385` el efecto de prellenado lleva `form.email.length` y `form.fullName.length` en su propio array de dependencias. Al borrar el ultimo caracter la longitud pasa a 0, el efecto se vuelve a ejecutar y reescribe el valor de la sesion. El arreglo es prellenar una sola vez -guarda de "ya sembrado" o dependencia solo de la sesion-, no ensanchar la condicion. Cierra con una prueba que borre el campo entero y afirme que sigue vacio tras el re-render. **ARREGLADO EL 2026-09-09.** El efecto ya no mira la longitud de los campos: siembra el valor de la sesion una sola vez y guarda en una referencia lo que sembro. Las dependencias quedan en `[session?.email, session?.displayName]`, y la guarda es `seededEmail.current !== email` en vez de `form.email.length === 0`. **Las dos mitades hacen falta y no son la misma:** quitar la longitud de las dependencias corta el re-render que reescribia el campo, y la referencia impide que un refresco de sesion que devuelve el mismo correo lo reescriba despues; ensanchar la condicion, que era la tentacion, no habria hecho ninguna de las dos. Un cambio de cuenta -correo distinto- si vuelve a sembrar, que es lo que un usuario espera al entrar con otra identidad. **Cinco pruebas nuevas en `apps/web/src/app/enrol/page.test.tsx`, cuatro de ellas rojas contra el codigo anterior:** vaciar Nombre y vaciar Email y seguir vacios tras escribir en otro campo, borrar tecla a tecla hasta la longitud 0 -que es el borde exacto donde fallaba, y que un `clear()` de golpe se salta-, y no resembrar un campo vaciado cuando la sesion se refresca sin cambios. La quinta afirma que la cortesia sigue viva: una sesion que llega despues del primer render -el caso real, porque el token se resuelve tarde- sigue rellenando los dos campos. Las doce pruebas que ya existian siguen verdes, incluida la que comprueba que el envio lleva `fullName` y `email` prellenados. `pnpm verify:mvp` en verde de punta a punta, codigo de salida 0. **DESPLEGADA Y VERIFICADA EN PRODUCCION EL 2026-09-09**, y sin ningun paso manual: **el push a `main` despliega la web**. Cloudflare Pages esta conectado al repositorio por integracion de git -no hay job de despliegue en `.github/workflows/`-, asi que el propio push del commit `8805ac3` creo el despliegue `e90663bf` en Production sobre la rama `main`. Verificado leyendo produccion, no el panel: `bptjersey.pages.dev/enrol` sirve exactamente el mismo conjunto de chunks que `e90663bf.bptjersey.pages.dev/enrol`, asi que el alias de produccion apunta a ese build. **Y la prueba que de verdad cierra la fila es el marcador del bug en el bundle servido:** en el build anterior -`a6d9da7`, despliegue `39dd9b30`, todavia en pie- el trozo que contiene el formulario (`2qwekmyf1297t.js`) lleva `email.length` dos veces y `fullName.length` otras dos, que son la guarda y el array de dependencias del efecto roto; en el build que produccion sirve hoy esos cuatro marcadores **no aparecen en ningun chunk**. `.length` sobrevive a la minificacion -es un nombre de propiedad-, asi que su ausencia es una afirmacion sobre el codigo que corre, no sobre el que esta en el repositorio. **No se probo el comportamiento pulsando el formulario real**, porque exige una sesion iniciada; lo que se afirma es que el codigo desplegado es el arreglado, y el comportamiento lo cubren las cinco pruebas. |
| T002V2 | Convertir el formulario en un asistente por pasos de 1 a 3 campos con barra de progreso | T001V2 | pendiente | El formulario actual presenta todos los campos de una vez (`apps/web/src/app/enrol/page.tsx`). Pasa a pasos de 3 campos como maximo, con barra de progreso y estado accesible en cada paso. La validacion se ejecuta por paso, no solo al enviar. Se conserva el estado ya escrito al retroceder. |
| T003V2 | Impedir el zoom automatico de iOS al enfocar un campo | T002V2 | pendiente | En iOS Safari el navegador hace zoom cuando el campo enfocado tiene un tamano de fuente menor de 16 px. La correccion es tipografica y de viewport, no `maximum-scale=1`, que rompe el zoom por gesto y con el la accesibilidad. Cierra con evidencia en un dispositivo o emulacion movil real. |

## V2-B - Reserva de la primera clase

| ID   | Tarea atomica | Depende de | Estado | Evidencia de salida |
| ---- | ------------- | ---------- | ------ | ------------------- |
| T004V2 | Unificar "Ask for a place" y "Book a free class" en el mismo formulario | T002V2 | pendiente | Bug confirmado: "Book a free class" apunta a `href="#contact"` en `apps/web/src/app/page.tsx:54` y `:230` -un ancla a su propia seccion, por eso no hace nada visible-, mientras "Ask for a place" va a `/enrol` (`page.tsx:227`). Los tres botones pasan a un unico destino: el formulario por pasos que reserva una clase gratis. Actualizar tambien `apps/web/src/lib/client-auth.tsx:195` y las pruebas de `test-harness.test.tsx` que hoy afirman el ancla. |
| T005V2 | Verificar que el waiver mostrado es el documento oficial, byte a byte | - | pendiente | **Auditado antes de abrir la fila: el texto ya coincide.** `packages/domain/src/consents/enrolment-waiver-terms.ts` contiene las diez clausulas del PDF oficial `Varios/Brazilian Power Team Jersey Waiver and Release of Liability.pdf`, con los mismos encabezados y con los bullets de higiene. La fila no construye el texto: comprueba mecanicamente que sigue coincidiendo. Cierra con una prueba que extraiga el texto del PDF y lo compare con `enrolmentWaiverTerms`, de modo que una divergencia futura falle en CI en vez de descubrirse en produccion. Si aparece divergencia, se acuna version nueva segun la regla ya escrita en la cabecera de ese archivo. |
| T006V2 | Reservar la primera clase antes de la aprobacion y notificar al administrador | T004V2 | pendiente | Aplica D3. El solicitante crea cuenta, reserva la clase gratis y la reserva dispara el aviso al administrador. Mientras esta pendiente ve su reserva y no puede reservar una segunda clase. Invierte el orden actual, en el que la aprobacion precede a cualquier reserva. Requiere un estado de cuenta "pendiente con reserva" en reglas y en los callables, no solo en la interfaz: la restriccion se aplica en el servidor. **Auditoria del 2026-09-09, que cambia el tamano de la fila:** reservar exige `studentId` y `membershipId`, y ninguno existe antes de la aprobacion. `requireStudentScope` (`apps/functions/src/schedule/schedule-callables.ts:64-82`) rechaza a `shopper`, y el resolutor canonico busca al alumno en `academies/{id}/students` o en `relationships`, colecciones que solo escribe la ruta de aprobacion. Entre `shopper` y `adultStudent`/`guardian` no hay estado intermedio: el claim salta de golpe. Invertir el orden no es mover una comprobacion, es crear la identidad de alumno antes de aprobar o abrir una ruta de reserva propia para la clase de prueba. **Interrogada y decidida el 2026-09-09, en la ronda de `/grill-me`:** la identidad sale de un **alumno provisional con membresia `trial`** creado en el registro, y la clase de prueba viaja por la **ruta de reserva unica**; el dominio ya lo admite, porque `evaluateBookingEligibility` acepta `trial` ademas de `active`, y asi el aforo, el quorum, el corte de cancelacion, la asistencia y el contador de T015V2 se aplican sin escribir nada. Se descarto la ruta de reserva propia justamente por eso: seria un segundo camino que toda regla futura tendria que acordarse de cubrir, y el que se olvidara no daria error, simplemente no se aplicaria. El provisional **no aparece en la base de miembros** hasta ser aprobado -el directorio canonico y `listMembers` lo excluyen por estado-, para que la cifra de miembros no se convierta en una cifra de interesados. Si la solicitud se **rechaza**, la membresia pasa a `cancelled`, alcanzable desde `trial`, de modo que la reserva y la asistencia pasadas conservan a quien referirse en vez de quedar huerfanas; **devolver al solicitante no cancela nada**, sigue pendiente. El limite de una sola clase es un **cupo de la propia membresia**, no una lectura del estado de la solicitud: el permiso lleva su limite dentro y no depende de dos documentos que puedan desincronizarse, y al aprobar la membresia pasa a `active` y el cupo deja de aplicar solo. La reserva de prueba **cuenta para el minimo de cuatro**, porque una plaza ocupada lo esta venga de quien venga y dos recuentos distintos sobre la misma sesion acabarian cuadrandose a mano. Y el aviso al administrador es **uno solo, en el momento de reservar**: con D3 el registro y la reserva ocurren seguidos, y dos avisos consecutivos sobre la misma persona son ruido que ensena a ignorarlos. |
| T007V2 | Cancelacion gratuita hasta 12 h antes; despues, propuesta de fee de inasistencia | - | pendiente | Aplica D2. Colision que hay que resolver con cuidado: hoy existe **un solo corte de una hora** en `packages/domain/src/schedule/schedule-contracts.ts:813` que gobierna a la vez el cierre de reservas, el cierre de cancelaciones y la cancelacion automatica por quorum (`:984`). Las 12 h son un tercer concepto distinto -cancelacion sin coste- y deben anadirse como corte propio, sin mover el corte de reservas ni el de quorum, que la cancelacion por quorum tambien usa. Cancelar dentro de las 12 h emite la propuesta de penalizacion de T111, revisable por un administrador. **Precisado el 2026-09-09:** el corte de una hora no es una constante, es el parametro por defecto `cutoffMinutes = 60` de `isWithinBookingCutoff` (`schedule-contracts.ts:816`), y las tres llamadas pasan `60` a mano (`booking-transaction-service.ts:598` reservas, `:806` cancelacion, `decideQuorumSweep` en `schedule-contracts.ts:1024`). Y el fee no esta limitado a Town por costumbre: esta clavado en el dominio. `noShowPenaltyLocationId = "town"` (`packages/domain/src/penalties/no-show-penalty-contracts.ts:14`) y `decideNoShowPenalty` devuelve `skipReason: "otherSite"` para cualquier otra sede; GBP 15 es `noShowPenaltyAmountMinor = 1_500`. La propuesta no la genera nada automatico: es el callable `proposeNoShowPenalties({ sessionId })`, que un miembro del personal ejecuta sesion por sesion. **Interrogada y decidida el 2026-09-09:** West **tambien** genera propuesta, y por el **mismo importe de GBP 15**, asi que `noShowPenaltyLocationId` deja de ser una constante y pasa a ser politica por sede. Esto **enmienda la decision 2 del BRIEF**, que limitaba el fee a Town sin dar ninguna razon -se leia como alcance de piloto-, y la enmienda queda escrita alli con fecha. **Precision que cambia la banda:** la cancelacion tardia no va de 12 h a 0, va de **12 h a 1 h**, porque esa misma decision 2 del BRIEF ya cierra la cancelacion una hora antes; dentro de la ultima hora no se puede cancelar en absoluto y quien no aparece cae por la via del no-show, que ya existe. La propuesta **nace en el instante de cancelar**, no en el barrido: `proposeNoShowPenalties` esta indexado por la asistencia de la sesion y una reserva cancelada no produce registro de asistencia, asi que esperar al barrido haria depender la propuesta de que alguien lo ejecute y de que sepa mirar donde no mira. Vive en **la misma coleccion `noShowPenalties`** con un motivo que la distingue de la ausencia sin avisar y de la perdida por contador de T015V2: la oficina las trata igual -mismo importe, misma decision de aprobar o descartar- y una sola cola es una sola pantalla. Y si la sesion se cancela despues por quorum, la propuesta **se retira automaticamente**: dejar viva una propuesta de cobro por una clase que no ocurrio es lo primero que un miembro disputaria, y retirarla no contradice D2, porque seguimos sin cobrar nada de forma automatica. |

## V2-C - Interfaz de miembros ya aceptados

| ID   | Tarea atomica | Depende de | Estado | Evidencia de salida |
| ---- | ------------- | ---------- | ------ | ------------------- |
| T008V2 | Permitir al representante marcar la asistencia de sus hijos al llevarlos a la practica | - | pendiente | Desde el area de familia (`apps/web/src/app/account/family/page.tsx`) el representante marca la llegada de cada hijo. La autorizacion se comprueba en el servidor contra la relacion tutor-menor, no en el cliente. Convive con el marcaje del staff de T014V2 sin duplicar asistencia. |
| T009V2 | Dar cuenta propia al menor, con correo o telefono aportado por el representante y alcance recortado | T008V2 | pendiente | Aplica D1. El representante anade un correo o un telefono al hijo; el menor inicia sesion y ve su progreso, sus proximas clases, reserva y marca asistencia. Nunca ve pagos, waiver, datos de otros miembros ni la ficha del representante. El representante conserva la titularidad economica y legal y puede revocar el acceso en cualquier momento. **Es tratamiento de datos de menores nuevo:** no cierra sin actualizar la DPIA y el registro de retencion de T011 y sin reglas de Firestore que nieguen por defecto todo lo que quede fuera del alcance recortado. **Auditoria del 2026-09-09:** hoy un menor no tiene cuenta de ninguna clase. `buildMinorStudent` (`apps/functions/src/families/family-service.ts:718-757`) nunca escribe `userId`, aunque el campo ya existe como opcional en `StudentProfile`, asi que el enganche a una cuenta esta previsto en el contrato. Los roles de cliente son solo `shopper`, `guardian` y `adultStudent`: hace falta uno nuevo. El unico umbral de edad del dominio es 18, en linea dentro de `deriveParticipantType` (`packages/domain/src/profiles/profile-contracts.ts:300-319`); no hay 16 en ninguna parte. La DPIA es un borrador sin aprobar (`docs/operations/t011-dpia-draft.md`) y el calendario de retencion (`t011-retention-residency-erasure-policy.md`, tabla en 99-112) advierte en su linea 7 de que ningun plazo esta implementado: hoy el sistema no borra nada al vencer. **Interrogada y decidida el 2026-09-09.** D1 **prevalece sobre la decision 6 del BRIEF**, que dice literalmente que los menores no tienen cuenta propia; esa decision se enmienda alli con fecha y motivo, sin borrarla, porque dos documentos de registro afirmando lo contrario sobre tratamiento de datos de menores es justo donde mas dano hace, y la DPIA va a citar ese parrafo. **Suelo de edad en 12**, reutilizando la linea que la academia ya traza entre Kids y Teens en esa misma decision 6, en vez de introducir un tercer numero; si la DPIA acaba apoyandose en consentimiento en lugar de en contrato, el suelo sube a 13, y eso lo fija la DPIA, no el codigo. **Hallazgo del 2026-09-09 que le quita una pata a D1 tal como estaba escrita:** «correo o telefono» hoy es solo correo. Los proveedores cableados son Google y correo/contrasena (`apps/web/src/lib/auth-client.ts:41-52`, `apps/web/src/lib/firebase-client.ts:224-230`); de telefono no hay nada, ni `signInWithPhoneNumber` ni `RecaptchaVerifier` en todo `apps/`. Y el proyecto **no puede enviar un correo a un tercero**: `ExternalDeliveryProvider` devuelve siempre `skipped` con `failureCode: "provider_unconfigured"` y no lo invoca ningun callable (`apps/functions/src/delivery/delivery-service.ts:129-142`); ademas ninguna funcion crea usuarios de Auth, siempre los crea el navegador del propio interesado. Por eso **la cuenta la crea el representante** con correo y contrasena y le entrega el acceso al hijo, y el backend engancha ese `userId` al `StudentProfile`, que ya prevee el campo; que el representante conozca la contrasena es aceptable porque sigue siendo el titular legal, pero **se declara en la DPIA** en vez de fingir que no pasa. **Revocar** quita el claim, desengancha el `userId` y **deshabilita la cuenta de Auth**: un token ya emitido sigue valiendo hasta caducar -el mismo problema que la puerta de administradores resuelve comprobando la cuenta viva en cada llamada-, y deshabilitar es reversible si el menor cumple 18 y pasa a alumno adulto. Se **construye ya**, pero **no se despliega hasta que la DPIA este aprobada** y la retencion deje de ser una promesa: asi la DPIA se escribe sobre un diseno real y nadie trata datos de un menor mientras tanto. |

## V2-D - Landing publica

| ID   | Tarea atomica | Depende de | Estado | Evidencia de salida |
| ---- | ------------- | ---------- | ------ | ------------------- |
| T010V2 | Actualizar la seccion de instructores | T021V2 | pendiente | Hoy `apps/web/src/content/academy.ts:154-168` lista a Miro, Eddie, Topo y Charlie. Se conservan Miro y Charlie; entran Amone Mouton, Catalina Bruma y Connor Hoopes. Bloqueada de hecho hasta T021V2: cada entrada del contenido exige `credential`, y las credenciales de los tres nuevos no las tenemos. No se inventan cinturones ni grados. |
| T011V2 | Publicar las dos direcciones de los centros de entrenamiento | T021V2 | pendiente | Hoy el contenido tiene **una sola** direccion (`academy.ts:48-53`, Town Office, Office 9, 13 Library Place, St Helier, JE2 3RR), consumida como objeto unico en `apps/web/src/app/page.tsx`. "BPT West / Strive" solo existe como etiqueta de tarifa (`academy.ts:144`), sin domicilio. Requiere convertir `location` en una lista de dos centros y actualizar el bloque `<address>` de la landing. Falta el domicilio de West, que aporta T021V2. |

## V2-E - Interfaz de administracion y entrenadores

| ID   | Tarea atomica | Depende de | Estado | Evidencia de salida |
| ---- | ------------- | ---------- | ------ | ------------------- |
| T012V2 | Reparar la cola de aprobacion de nuevos miembros | T025V2 | desplegada | **La fila mas urgente del ledger: hoy no se puede dar de alta a nadie.** **Diagnosticado en produccion el 2026-09-09; la causa no era ninguna de las cuatro que la fila listaba.** Los registros de Cloud Run dicen `Callable request verification passed` y acto seguido **HTTP 403** en `getEnrolmentRequestDetail`, y el mismo 403 aparece en `listMembers`, `getMemberDetail` y `lookupMemberIdentity`, mientras `listEnrolmentRequests` responde 200 en la misma sesion. Lo que separa a unas de otras es la puerta: las que fallan pasan por `requireCanonicalMemberDirectoryActor` (`apps/functions/src/members/canonical-actor.ts:81`), que ademas del claim exige un documento de personal aprovisionado en `academies/{academyId}/users/{uid}`; las que funcionan solo miran el claim del token. En produccion hay dos cuentas administrativas: la de `owner`, con su documento completo -verificado contra el propio esquema, pasa la puerta-, y la de `administrator`, **con claims validos y sin documento ninguno**. Por eso la cola se pinta y ninguna fila se abre. Los tres botones no estan inertes: "Read the full request" recibe el 403, "Approve and enrol" esta `disabled` a proposito hasta que el detalle cargue (`requests/page.tsx:309-312`), y "Send back to applicant" si funciona -usa la puerta del claim- pero exige escribir la nota antes. Descartados con evidencia: secretos (los tres estan enlazados en version 2 y las funciones estan `ACTIVE`), App Check (su ausencia da 401, visible en los registros del 2026-09-07), claims (satisfacen la puerta que devuelve 200) y reglas de Firestore (el Admin SDK no las evalua). **Brecha estructural:** el unico escritor de ese documento es `provisionAdminRole` (`apps/functions/src/auth/admin-provisioning.ts:506`), que `index.ts` reexporta como funcion suelta y no como `onCall`, asi que no esta desplegada: hoy no existe camino en produccion para aprovisionar a un administrador. Hecho el 2026-09-09: el cliente ya no colapsa el 403 en una sola frase ciega (`enrolment-client.ts`, `officeFailure`) y hay pruebas que fijan la asimetria de las dos puertas. **Decidido por el operador el 2026-09-09 (D5): no se escribe nada en produccion.** La cola se opera desde la cuenta de `owner`, que ya pasa la puerta. Descartar el aprovisionamiento a mano tiene dos razones concretas, y no solo el pudor de tocar produccion: el documento se valida contra un `z.strictObject` de catorce campos (`apps/functions/src/members/member-directory-actor-authorization.ts:4-20`) -dos `Timestamp` reales, `authProvider: "google"`, `active: true`, `status: "active"`, `schemaVersion: 1` y un `lastRoleChangeAuditId`-, de modo que un campo de mas o de menos deja el 403 intacto; y `provisionAdminRole` no escribe solo ese documento, lo escribe dentro de una transaccion que toma el cerrojo de rol y emite el evento `admin.role.granted` (`apps/functions/src/auth/admin-provisioning.ts:497-546`), asi que un documento tecleado por consola seria valido para la puerta y no dejaria rastro de quien concedio ese poder. **Limitacion conocida, declarada y no olvidada:** la cuenta de `administrator` (uid BIuILH...) sigue sin poder abrir ninguna fila del directorio canonico hasta que exista un camino auditado, que es T022V2. Falta lo unico que cierra esta fila: aprobar a un solicitante real de punta a punta desde la cuenta de `owner`. Es una accion en la sesion de produccion del operador, no algo que el repositorio pueda hacer por el. No se cierra con un mensaje de error mas bonito. **Corregido mas tarde el mismo 2026-09-09: el diagnostico estaba incompleto y la fila culpaba a quien no era.** Probado en produccion desde la cuenta de `owner`, la que eligio D5: el detalle **si** abre -`enrolmentRequestDetail` pasa `requiresCanonicalReader: false` (`canonical-member-directory-read-service.ts:886`) y se salta la comprobacion de estado del directorio-, pero **aprobar falla igual**, con `approval_write_failed`. La causa no es el documento de personal que le falta a `administrator`: es que **el directorio canonico de miembros nunca se inicializo en produccion**, y sin su documento de estado ni la ruta de lectura ni la de escritura funcionan para nadie. Eso es T025V2, y es lo que de verdad impide dar de alta. Lo diagnosticado sobre `administrator` sigue siendo cierto y sigue devolviendo 403, pero aprovisionarlo no habria dado de alta a nadie tampoco: la fila se equivocaba de culpable, no de sintoma. Esta fila no puede cerrarse antes que T025V2. **CERRADA EL 2026-09-09, con una aprobacion real de punta a punta.** Con el directorio ya inicializado, `approveEnrolmentRequest` devuelve **200** -`role: adultStudent`, un `studentId` nuevo, `alreadyApproved: false`- y la solicitud pasa de `approval-failed` a `approved`: el reintento tras un fallo funciona, tal y como T025V2 anticipaba. Verificado en produccion, el directorio canonico pasa de vacio a **un estudiante escrito de forma coherente en las tres colecciones** -`students`, `studentIdentityKeys` y `studentAdminProfiles`, una fila cada una-, `users` sube de 1 a 2 con la cuenta cliente del solicitante, y `auditEvents` de 22 a 27. Hoy se puede dar de alta. **Un hallazgo del camino, que no es un defecto y conviene no olvidar:** el primer intento fue sobre la solicitud de *Andres Santiago* y devolvio `400 FAILED_PRECONDITION: applicant_account_unavailable`. La causa se verifico con `accounts:lookup`: **la cuenta de Auth que envio esa solicitud (`lXadgEEQQqdUG3QmxA1q3rEDLUs1`) ya no existe**. El servicio esta negandose a dar de alta a alguien cuya cuenta se borro, que es lo correcto; pero esa fila de la cola **no se podra aprobar nunca** mientras siga asi, y hoy la interfaz no ofrece mas salida que devolverla al solicitante. Las otras dos cuentas existen y estan activas. **Limitacion que sigue en pie:** la cuenta de `administrator` continua sin poder abrir el directorio canonico, que es lo que levanta T022V2. |
| T013V2 | Poner las sesiones del dia como primer bloque del panel de administracion | T012V2 | pendiente | El panel (`apps/web/src/app/admin/overview-page.tsx`) ya tiene "Today's classes"; la fila lo eleva a primer bloque y expone las sesiones que quedan por delante en el dia con su hora de inicio. |
| T014V2 | Priorizar el boton de asistencia 15 minutos antes de cada sesion | T013V2 | pendiente | A falta de 15 minutos para el inicio, el panel asciende un boton de asistencia. Marcar se hace seleccionando de la lista de reservados quien esta y quien no, en una sola pantalla, sin buscar miembro por miembro. |
| T015V2 | Contador de 20 minutos: quien no marca asistencia pierde la clase automaticamente | T014V2 | pendiente | Quien no marco antes del inicio entra en un contador; a los 20 minutos del comienzo sin marcaje, pierde la clase automaticamente. **Open mat queda excluido por completo.** El vencimiento lo decide el servidor por hora de sesion, no un temporizador del navegador, que se pierde al cerrar la pestana. Interactua con T007V2: hay que decidir explicitamente si esta perdida automatica emite o no propuesta de fee, y dejarlo escrito en la fila antes de implementarla. **Dos hallazgos del 2026-09-09 que cambian lo que cuesta la fila.** Primero, y **corregido el mismo dia porque la fila lo afirmaba mal**: es cierto que `SessionRecord` (`packages/domain/src/schedule/schedule-contracts.ts:165`) no tiene campo de tipo y que `sessionTypes = ["class", "openMat"]` (`packages/domain/src/memberships/plan-contracts.ts:27`) no sale del dominio de planes, pero de ahi no se sigue que la exclusion no se pueda expresar: **ya se deriva y ya se usa**. La sesion lleva `programId`, `ProgramRecord` lleva `discipline`, `"open-mat"` esta en el vocabulario de disciplinas, hay un programa semilla `open-mat`, y la ruta de reserva ya hace exactamente esa derivacion en `apps/functions/src/schedule/booking-transaction-service.ts:680`. Excluir open mat no exige tipar la sesion ni migrar nada. Segundo, no existe ningun barrido programado en el proyecto: las dos unicas funciones `onSchedule` limpian sesiones de importacion de miembros, y hasta el barrido de quorum es el callable manual `reconcileSessionQuorum`. Asi que «lo decide el servidor por hora de sesion» exige elegir entre anadir una funcion programada o evaluar el vencimiento en lectura. La asistencia, ademas, no es un campo de la reserva: es un registro aparte con su propio vocabulario (`attendanceStates`, `:1039`), donde `no_show` ya existe. **Interrogada y decidida el 2026-09-09:** la perdida a los 20 minutos **emite la misma propuesta** que un no-show, en la misma coleccion `noShowPenalties` y con su propio motivo, porque economicamente es identica -la plaza quedo retenida hasta pasado el inicio y ya no la uso nadie- y `no_show` ya existe en `attendanceStates`; sigue siendo propuesta revisable, nunca cobro, y **nunca en open mat**. El vencimiento lo decide una **funcion programada**, no una evaluacion en lectura: al haber decidido que emite propuesta, la via perezosa obligaria a **escribir durante las lecturas**, y ademas las cinco rutas transaccionales que leen sesiones para escribir no pasan por `ScheduleStore`, asi que el punto unico de intercepcion solo cubriria la mitad del problema. La exclusion de open mat **se deriva del programa**, sin anadir campo a la sesion: un tipo copiado se queda obsoleto si alguien cambia la disciplina del programa, y tendrias dos verdades sobre la misma sesion. El alcance de la funcion programada es **solo el contador**; programar el barrido de quorum es T024V2, fila propia. |
| T016V2 | Mostrar el calendario semanal al estilo Regyfit | T013V2 | pendiente | Vista semanal completa en el panel, no solo el dia en curso. |
| T017V2 | Ver la siguiente clase con los confirmados y avisar de condiciones medicas | T013V2 | pendiente | Lista de quien ya confirmo para la siguiente sesion y, para quien participe ese dia, aviso con el **nombre de la condicion y el miembro**. Dato de salud: solo lo ve el personal autorizado, cada lectura queda auditada y el aviso no se filtra a vistas agregadas ni a exportaciones. Se apoya en los datos de salud ya existentes; no cierra sin revisar su gate de acceso. |
| T022V2 | Desplegar `provisionAdminRole` como callable, con su revision de autorizacion | - | desplegada | Aplica D6. Hoy `apps/functions/src/auth/admin-provisioning.ts:677` es el unico escritor del documento de personal que exige la puerta canonica, y `index.ts:11` lo reexporta como funcion suelta y no como `onCall`: no esta desplegada, asi que en produccion no existe ninguna via para aprovisionar a un administrador. **No es reexportar envuelto en `onCall`, y conviene decirlo antes de estimar la fila:** la firma recibe el objetivo como segundo parametro de la funcion, no en `request.data`, y `provisioningRequestSchema` es un `z.strictObject({ action })` (`admin-provisioning.ts:78`) que rechaza cualquier campo extra, asi que hoy `uid`, `email` y `role` no caben en la peticion. Convertirla en callable es ensanchar el contrato de entrada de la superficie de autorizacion. Lo que ya trae hecho: `requireAdminActor` mas `requireOwner` (`:589-590`), de modo que solo `owner` concede, y un `administrator` no puede ascender a nadie ni a si mismo. Lo que le falta frente a la puerta hermana: `requireCanonicalMemberDirectoryActor` verifica App Check **en el manejador** (`canonical-actor.ts:83-85`) y esta no lo hace; desplegarla sin esa comprobacion abriria una puerta mas debil que la que protege lo que concede. Cierra aprovisionando de verdad por esa via a una cuenta que pueda pasar la puerta, y comprobando que despues abre una fila de la cola: es exactamente la limitacion que D5 dejo declarada en T012V2. **Corregido el 2026-09-09, y cambia el objetivo de la fila: esa cuenta no puede ser la de `administrator`.** Leyendo Auth, `admin@admin.com` (uid `BIuILHd7xnT3FwPwKrtBH21SnU63`) tiene **proveedor `password` y ningun `displayName`**. El documento de personal que exige la puerta se valida contra un `z.strictObject` con `authProvider: z.literal("google")` y `displayName: z.string().min(1)` (`member-directory-actor-authorization.ts:4-20`), y el propio `provisionAdminRole` exige lo mismo por su lado con `requireGoogleUser`. **Asi que esa cuenta no puede tener acceso funcional al directorio por ninguna via**, ni la auditada ni a mano: escribir `authProvider: "google"` para una cuenta de contrasena seria meter una afirmacion falsa en el registro de autorizacion, que es justo lo que ese esquema existe para impedir. La segunda cuenta operativa tiene que ser **una cuenta de Google**, y el operador aun no ha dicho cual. Mientras tanto, D8 le subio el claim a `owner` a esa cuenta, sabiendo que el directorio le seguira negando el paso. **CODIGO HECHO EL 2026-09-09; faltan el despliegue y un dato del operador.** `provisionAdminRole` pasa a `export const provisionAdminRole = onCall(browserAdminCallableOptions, ...)`, asi que `index.ts` la despliega en vez de reexportarla suelta: el detector del release delta la mueve de la lista de saltadas a la de funciones, y `qa/unit/release-delta.test.ts` afirma ese cambio de lado, que es lo que significa la fila. El contrato de entrada se ensancho con una **union de dos formas estrictas**, no relajando la que habia: `{ action }` -el objetivo por parametro, la costura interna que ya estaba probada- y `{ action, uid, email, role }` -todo en la peticion, que es lo unico que un navegador puede mandar-. Las dos siguen rechazando cualquier campo de mas, asi que una peticion a medio camino como `{ action, uid }` no encaja en ninguna de las dos y se rechaza, en vez de colarse por la mas permisiva; y `action` es obligatoria en la forma del navegador para que una concesion de poder no pueda salir de una omision. App Check se verifica en el manejador **dos veces a proposito**: al principio de `provisionAdminRoleHandler`, para que un cliente sin atestiguar se rechace por lo que es y no por lo que manda, y dentro de `provisionAdminRoleWithServices`, que es la costura que alcanza cualquier otra via -una prueba, otro modulo, un callable futuro que se olvide de la opcion-. **No se copio la sonda de actividad de la puerta hermana, y la razon importa:** `createMemberDirectoryActorActivityCheck` exige que quien llama tenga ya documento de personal, y esta funcion es el unico escritor de ese documento, asi que exigirlo dejaria a una academia nueva sin poder conceder nunca el primer rol. La diferencia queda declarada, no heredada por descuido. Pruebas: 32 en `apps/functions/src/auth/admin-provisioning.test.ts`, seis de ellas nuevas -rechazo sin App Check con cuerpo valido, objetivo leido de `request.data` al conceder y al revocar, siete cuerpos a medio formar rechazados uno a uno, un `administrator` que no asciende a nadie ni a si mismo, y la forma `onCall` declarada-. La atomicidad que pedia el quinto requisito ya estaba probada y se cita en vez de duplicarse: al fallar Firestore, `services.firestore.records` queda vacio del todo, ni documento de personal ni evento, y al conceder los dos salen de la misma transaccion enlazados por `lastRoleChangeAuditId`. `pnpm verify:mvp` en verde de punta a punta, codigo de salida 0: 2557 pruebas unitarias, 92 de reglas, humo e2e y carga sintetica. **DESPLEGADA EN PRODUCCION EL 2026-09-09**, desde el commit 626170f y por la misma via que T025V2: `functions.configDir` en una configuracion temporal que apunta el dotenv fuera del directorio que borra el predeploy, con `BPT_WAIVER_REGISTRATION=disabled`, que es el valor que produccion ya llevaba; no se commiteo ningun valor del gate del waiver y la configuracion temporal se borro al terminar. Evidencia: el inventario pasa de 71 a 72 funciones y el log del despliegue solo registra `creating ... provisionAdminRole` seguido de `Successful create operation`, sin ninguna retirada; `provisionAdminRole` aparece como callable v2 en us-central1 sobre nodejs22; `gcloud functions describe` la da en estado ACTIVE; y el sondeo anonimo -POST con un cuerpo bien formado y sin token- devuelve 401 `UNAUTHENTICATED`, que es la puerta cerrandose antes de mirar el cuerpo, tal y como se escribio. **Ya existe una via en produccion para aprovisionar a un administrador**, que es lo que la fila levantaba de D5. **Lo unico que queda, y por eso la fila pasa a bloqueada:** elegir la cuenta de Google que sera el segundo owner. Es un dato que solo tiene el operador, no una decision que se pueda tomar leyendo el codigo, y sin ella no hay a quien aprovisionar ni con quien comprobar que despues abre una fila de la cola. En cuanto el operador la nombre, la fila se cierra en una sola sesion: aprovisionar por la callable ya desplegada y abrir una fila de la cola desde esa cuenta. **Retomada el 2026-09-09: pasa a `en-progreso` para reservarla frente al trabajo en paralelo.** El estado no dice que el dato haya llegado, dice que esta sesion la esta trabajando; los dos requisitos que siguen abiertos -elegir la cuenta de Google y aprovisionarla- necesitan el correo del operador y siguen sin marcar. **El operador nombro una cuenta el 2026-09-09, y leyendo produccion resulta que no puede serlo: es el estudiante que T012V2 dio de alta.** Leido con `accounts:lookup` y con la API de Firestore, las tres cuentas de Auth quedan asi: la de `owner` real es una cuenta de Google con `displayName`, su documento de personal completo -`accountType: staff`, `authProvider: google`, `adminRole: owner`, `createdBy: system:admin-bootstrap`- y es la que pasa la puerta canonica; `admin@admin.com` sigue con proveedor `password` y sin `displayName`, como ya decia la fila; y la cuenta nombrada (uid `BZrK8L...`) es de Google y tiene `displayName`, **pero su claim es `role: adultStudent` y su documento en `academies/demo-academy/users/{uid}` es `accountType: client`, creado el 2026-09-09 por el `owner`**: es exactamente la cuenta cliente del solicitante que la aprobacion de punta a punta de T012V2 escribio. **La callable la rechazaria, y el rechazo es correcto:** `requireTargetAdminClaims` (`admin-provisioning.ts:193-209`) ve que el objetivo ya trae `academyId` y `role`, los pasa por `parseAdminClaims`, y `adultStudent` es un `userRole` valido que **no** esta en `administrativeRoles` (`packages/domain/src/actor-context.ts:19`), asi que devuelve `ADMIN_ROLE_INVALID` y el manejador lanza `permission-denied`. El mensaje que sale, *"Target belongs to another academy"*, describe mal la causa -la academia coincide; lo que no coincide es el rol- y conviene no creerselo cuando aparezca. **Y si esa puerta no estuviera, el dano seria real:** el documento de personal se escribe con `transaction.set(userRef, {...})` sin `merge` (`admin-provisioning.ts:574-590`), sobre **la misma ruta** donde vive el documento cliente del estudiante, asi que la concesion lo reemplazaria entero -`accountType` pasaria de `client` a `staff` y el telefono del miembro desapareceria- y el claim `adultStudent` quedaria sobrescrito por `owner`, dejando sin rol de estudiante al unico alta real que tiene la plataforma. La fila no se cierra con esta cuenta. **Lo que hace falta es una cuenta de Google distinta**, y aqui esta la pregunta que el hallazgo abre y que no se puede responder leyendo el codigo: toda cuenta que haya usado la plataforma como cliente lleva ya un claim no administrativo, asi que los unicos objetivos que la callable acepta hoy son cuentas **sin claim ninguno** o cuentas que ya son administrativas. No hay via auditada para ascender a un cliente, y decidir si debe haberla es una decision de producto, no un arreglo. Buscadas y no encontradas: ninguna via de baja, borrado ni anonimizacion de miembro en `apps/functions` ni en `packages/domain`. Y el modelo no admite el termino medio: `academies/{academyId}/users/{uid}` es **un** documento con `accountType` que es `client` **o** `staff`, de modo que una misma persona no puede ser estudiante matriculado y owner a la vez. **Resuelto por el operador el 2026-09-09 (D9): el acceso administrativo se queda en las dos cuentas que ya existen.** **CERRADA EL 2026-09-10, ejerciendo la callable de verdad en produccion.** Faltaba lo unico que el despliegue no demostraba: que concediera. La evidencia que habia era un `401` a un sondeo anonimo, es decir la puerta cerrandose, no la funcion funcionando. **Hallazgo del camino, y no es menor: la web no tiene ninguna pantalla que llame a `provisionAdminRole`** -cero coincidencias en `apps/web/src`-, asi que el contrato se ensancho "para lo que un navegador puede mandar" y no hay navegador que lo mande. La llamada se hizo reutilizando las cabeceras reales de otra callable de la misma sesion de `owner` -`Authorization` y `X-Firebase-AppCheck`-, que es la unica forma de pasar App Check de produccion desde fuera de la aplicacion. **El objetivo fue la propia cuenta de `owner`, y es la unica que podia serlo:** `admin@admin.com` la rechaza `requireGoogleUser` por ser de contrasena -tanto al conceder como al revocar-, y ascender a un cliente lo prohibe D9. Es ademas la operacion mas segura posible sobre esa superficie, porque `nextClaims` queda identico a `previousClaims` y la mutacion de Auth es un no-op. Respuesta: **HTTP 200**, `{uid, role: "owner", action: "grant"}`. Verificado leyendo Firestore despues: `lastRoleChangeAuditId` pasa de `AyCuT8UhBg8U04sZ0d3S` a `SBtdFTLdc0FZIOc8BOea`, `updatedBy` de `system:admin-bootstrap` al uid del owner y `updatedAt` de 2026-09-07 a `2026-09-10T02:18:49.352Z`, mientras `createdAt` y `createdBy` se conservan y los quince campos siguen intactos -el `set` sin `merge` no perdio ninguno-; y el evento `admin.role.granted` existe con ese id exacto, con `actorId` y `targetRef` apuntando al owner y el mismo `occurredAt` al milisegundo que el documento, que es lo que prueba que salieron de la misma transaccion. Ya existe una via auditada y **demostrada** para conceder un rol administrativo en produccion. **Lo que esta fila NO arregla, y conviene no leerlo como cerrado:** no hay un segundo owner, la limitacion de D5 sigue en pie y el trabajo administrativo real depende de una sola cuenta. Eso pide una cuenta de Google que no haya sido cliente, o una decision de producto sobre las cuentas de contrasena; ninguna de las dos cabe en esta fila. |
| T024V2 | Programar el barrido de quorum, que hoy no se ejecuta nunca | T015V2 | pendiente | Sale de la ronda del 2026-09-09, y no es una mejora: es el incumplimiento de una decision aprobada. La decision 3 del BRIEF promete que, si una sesion no reune cuatro reservas una hora antes, «una tarea idempotente cancela la sesion y emite avisos in-app». Esa tarea **no ocurre en produccion**. `reconcileSessionQuorum` es un callable de staff al que **ningun cliente web llama** -`apps/web/src/lib/schedule-client.ts` no lo expone-, y el unico runner en lote se declara en su propia cabecera como deliberadamente no programado, con `assertQuorumSweepRunnerEnvironment` restringiendolo al emulador demo (`apps/functions/src/schedule/quorum-sweep-runner.ts:8-15`, `:74`). La regla pura `decideQuorumSweep` y el servicio transaccional idempotente ya existen y estan probados: lo que falta es que algo los dispare. Depende de T015V2 porque reutiliza la maquinaria programada que aquella introduce. Se abre como fila propia, y no de polizon dentro de T015V2, porque incumplir una decision aprobada del BRIEF merece su propia evidencia y su propia aprobacion. |
| T025V2 | Inicializar el directorio canonico de miembros en produccion, que nunca se inicializo | - | desplegada | **Causa raiz real del alta rota, encontrada el 2026-09-09, y mas grande que la que T012V2 tenia escrita.** Verificado leyendo produccion: la coleccion `academies/demo-academy/memberDirectoryStates` esta **vacia** y el documento `current` que toda lectura y toda escritura del directorio exigen **no existe**. La lectura lo pide en `assertCanonicalReader` (`apps/functions/src/members/canonical-member-directory-read-service.ts:302-343`) y lanza `unavailable`, que Firebase mapea a **HTTP 400**: es el 400 de `listMembers` que aparece en la consola desde una sesion de `owner`. La escritura lo exige ademas junto al documento de guarda `memberDirectoryRestoreGuards/{academyId}` (`canonical-member-directory-service.ts:776-793`), y por eso `approveEnrolmentRequest` muere y el cliente muestra `approval_write_failed: The enrolment could not be completed`. **No es un problema de la cuenta, es de la academia:** afecta igual a `owner` y a `administrator`, de modo que aprovisionar al administrador nunca habria bastado. **Y hoy no existe ninguna via para arreglarlo:** el inicializador es de emulador por diseno en cuatro capas -la constante `projectId = "demo-bpt-jersey"` (`member-directory-empty-initializer.ts:18`), el `z.literal` del mismo valor en el esquema del artefacto (`:27`), `target: "emulator"` en la comprobacion de entorno (`:182`) y el rechazo `store.projectId !== projectId` (`:206`)-, su ejecutor clava el mismo proyecto (`apps/functions/scripts/member-directory-empty-initialize.mjs`) y `index.ts` no lo exporta a proposito. Exige ademas que dieciocho colecciones esten vacias, incluida `auditEvents`, que produccion ya tiene, asi que la precondicion actual lo bloquea aunque se le cambiara el proyecto. **Dato operativo que tranquiliza:** un intento fallido deja la solicitud en `approval-failed`, que **sigue siendo aprobable** (`packages/domain/src/members/enrolment-request-contracts.ts:460-461`), asi que no hay nada atascado y reintentar funcionara en cuanto exista el documento. **Decidido por el operador el 2026-09-09: se construye la via de produccion, no se escribe a mano.** **Construido y probado el mismo dia.** El guardarrail de emulador se deja intacto: en vez de debilitarlo, se anade una via propia, `apps/functions/src/members/canonical-directory-initialization.ts`, con su adaptador de Firestore y el callable `initializeCanonicalMemberDirectory`, solo para `owner` y con App Check. Escribe el estado, la guarda y su evento cero **en una sola transaccion, create-only**, y anade el evento de auditoria `member.directory.initialized` en la misma: una inicializacion sin rastro seria justo el acto inatribuible que el modelo de integridad existe para impedir. La academia sale del claim verificado y nunca del payload, que ademas debe venir vacio. La precondicion de vacuidad pasa de dieciocho colecciones a **diez**, las que de verdad harian falsa la frase "directorio vacio"; `auditEvents` sale de la lista, porque es un registro de solo anadir que describe intentos y no estado, y `members` tambien, por la decision D7 que se explica al final de esta fila. **Dos correcciones que las pruebas obligaron y que conviene registrar, porque yo las tenia mal:** primero, el artefacto de linea base **no** era prescindible -el esquema de estado rechaza `identityKeyCoverage: "complete"` sin `identityKeyBaselineMac` ni `identityKeyBaselineArtifactId`, y sin cobertura completa ni el lector ni el escritor aceptan el estado-, asi que la linea base vacia se deriva aqui byte a byte como la deriva el flujo del artefacto, con los mismos separadores de dominio y el mismo secreto de integridad; y segundo, se recupera la comprobacion de entropia del secreto, que un secreto de un byte repetido es un secreto sin poner disfrazado de secreto. **Ademas se deja de esconder la causa:** `listMembers` colapsaba cualquier fallo en "Unable to load members. Please try again." y por eso el 400 solo se veia en la consola del navegador; ahora nombra las dos causas accionables con tipos propios, y la pagina de miembros ofrece el boton de inicializar unicamente cuando esa es la causa. La pagina sigue sin renderizar un mensaje arbitrario -una prueba existente cazo esa regresion mientras se construia-, asi que solo llegan a pantalla los mensajes que reconoce. Todo con pruebas: 14 de la logica de inicializacion, 15 del cliente y 8 de la pagina; la suite completa pasa con 2527. **El 2026-09-09, preparando el despliegue, la sonda de produccion encontro lo que faltaba por medir y habria hecho fallar el boton.** `academies/demo-academy/members` **no esta vacia**: tiene **243 registros reales**, importados del PDF el 2026-08-13 -`source: "member-pdf-import"`, con su evento `member.import.confirmed`-, y `members` era la primera de las once colecciones que la precondicion exigia vacias. Pulsar el boton habria devuelto `failed-precondition` nombrando esa coleccion: no un fallo de despliegue, sino la precondicion haciendo su trabajo sobre una premisa equivocada. **La premisa equivocada era mia:** `members` no es el directorio canonico. El canonico vive en `students`, `studentIdentityKeys` y `studentAdminProfiles` -las tres verificadas vacias en produccion, igual que `memberDirectoryStates`-, y `members` es la **coleccion origen** que la migracion forward lee (`sourceCollection: "members"`, `member-directory-forward-executor.ts:193`). Y esa migracion exige `identityKeyCoverage: "complete"` con su linea base (`assertMemberDirectoryForwardBaseline`, `member-directory-forward-runner.ts:142-156`) y el documento de estado (`member-directory-forward-runner-firestore.ts:114`), que es exactamente lo que solo el inicializador escribe. **Con `members` en la lista, la academia no podia inicializar ni migrar: cerrojo circular, y los 243 sin camino para siempre.** Decidido por el operador el mismo dia (D7): `members` sale de la precondicion, por el mismo razonamiento que saco a `auditEvents`, y quedan diez. Con una prueba propia que lo fija, 15 en el modulo. **Consecuencia aceptada a sabiendas:** tras inicializar, `/admin/members` mostrara **cero miembros** hasta que los 243 se migren, y hoy no hay ninguna callable de migracion desplegada -verificado leyendo `functions:list`-. Eso no es una regresion: es el estado real del directorio canonico, que hasta ahora ni siquiera se podia leer. **DESPLEGADA EN PRODUCCION el 2026-09-09**, desde el commit `b033c23`, con el arbol limpio y `pnpm verify:mvp` en verde -2528 unitarias, 287 ficheros, 13 de Rules-. Verificacion posterior del runbook §4.4: el inventario pasa de **70 a 71 funciones**, ninguna retirada, `initializeCanonicalMemberDirectory(us-central1)` en estado `ACTIVE` sobre `nodejs22`, ligando `MEMBER_DIRECTORY_MIGRATION_INTEGRITY_SECRET@2` -la misma version que ligan `listMembers` y `approveEnrolmentRequest`, asi que el MAC que escriba sera el que esos lean-, y el sondeo anonimo devuelve **401**, que es la respuesta correcta: ni 404 de no desplegada ni 5xx de arranque roto. **El limite que lo bloqueaba, y como se resolvio sin tocar la decision pendiente.** `firebase deploy` se planta en el parametro `BPT_WAIVER_REGISTRATION` en cualquier sesion no interactiva: fallo dos veces aqui, la segunda pasandolo como variable de entorno. Leyendo el CLI se confirma por que, y es peor de lo que parecia -`params.js:180` lanza el error para **cualquier** parametro que no venga en `userEnvs`, tenga o no `default`, asi que el `default: "disabled"` no sirve de nada sin TTY-. Pero el mismo codigo enseña una via soportada que no se habia probado: **`functions.configDir`** (`prepare.js:198`) apunta la busqueda del dotenv a un directorio **distinto de `source`**, y por tanto a uno que `clean-deploy-target.mjs` no borra. Se desplegó con una configuracion temporal identica a `firebase.json` salvo esa clave, apuntando a un dotenv fuera del repositorio con `BPT_WAIVER_REGISTRATION=disabled` -el valor que produccion ya llevaba-, y se borro despues. **Eso no consume la decision que `tasks.md` dejo abierta**, que es si el artefacto debe emitir el dotenv desde un valor **versionado**: aqui no se commiteo ningun valor ni se cambio el constructor del artefacto. Lo que si conviene actualizar es la afirmacion de `tasks.md` de que las tres vias estaban probadas: habia una cuarta. **PULSADO EN PRODUCCION el 2026-09-09 desde la cuenta de `owner`, y fallo. El fallo era mio, y es el hallazgo mas util del dia.** La pagina hizo su parte entera -sesion `OWNER ACCESS`, `listMembers` devolviendo `400 FAILED_PRECONDITION` con `Member directory is unavailable`, el mensaje que nombra la causa y el boton delante-, y App Check y Auth se verificaron `VALID` en el registro. Pero la callable devolvio **`400 INVALID_ARGUMENT: Invalid audit event draft`**, y la transaccion no escribio nada: `memberDirectoryStates` seguia en 0 y `auditEvents` en 21. **Causa:** la accion `member.directory.initialized` **no estaba en el catalogo `auditActions` del dominio** (`packages/domain/src/audit/audit-event.ts`), asi que `parseAuditEventDraft` la rechazaba y `appendAuditEventInTransaction` lanzaba `invalid-argument` -el mismo evento de auditoria que esta fila presumia de anadir era lo que impedia inicializar-. **Por que no lo cazo nada:** el adaptador construia el borrador con `as unknown as AuditEventDraft`, un doble casteo que silenciaba el unico error que importaba, y la unica prueba que tocaba el tema afirmaba el **nombre** de la accion contra un almacen falso, que siempre le da la razon. Ni el compilador ni las 2528 unitarias podian verlo; hizo falta pulsarlo en produccion. **Arreglado el mismo dia, en tres piezas:** la accion entra en `auditActions`, en la union de acciones simples y en `fieldsByAction` -el `Record<AuditAction, ...>` obliga a lo tercero, y el contrato publico de `contracts.test.ts` obliga a declararla-; el casteo desaparece a favor de `buildInitializationAuditDraft`, exportado y tipado de verdad, de modo que una accion que el dominio no conozca deja de compilar; y una prueba nueva pasa ese borrador por `parseAuditEventDraft`, el parser real, en vez de por un doble. El modulo pasa de 15 a 16 pruebas. **HECHO EL 2026-09-09.** Redesplegada y pulsada desde la cuenta de `owner`: `initializeCanonicalMemberDirectory` devuelve **200** con `{academyId: "demo-academy", alreadyInitialized: false}`, y `listMembers` pasa en la misma pantalla de `400 FAILED_PRECONDITION` a **200** con cero filas. Verificado leyendo produccion, los cuatro documentos comparten timestamp exacto -`2026-09-09T20:52:46.963207Z`-, que es la prueba de que la transaccion fue una sola: el estado con `identityKeyCoverage: complete`, `secretVersion: identity-v1` y su linea base `empty-baseline-70bf29b6...`; la guarda; su evento cero; y el evento de auditoria `member.directory.initialized`, cuyo actor es el uid del `owner` y cuyo objetivo es el propio documento de estado. La inicializacion quedo atribuida, que era justo el punto. **Cero miembros en pantalla es el resultado correcto**, no una regresion: los 243 del PDF siguen siendo legacy y su migracion es otra fila. Eso desbloqueo T012V2. |
| T026V2 | Decir quien ha iniciado sesion en la cabecera: nombre, rol y correo | - | revision | Pedido por el operador el 2026-09-10. Hasta ahora la cabecera decia `Authenticated shell - Owner access` (`apps/web/src/app/admin/admin-shell.tsx:267`): el rol si, la persona no. En una plataforma donde **cada cambio se registra con quien lo hizo**, no saber con que cuenta estas dentro es un riesgo real de operar con la equivocada, y este ledger ya tiene el caso: la sesion del 2026-09-09 confundio dos cuentas de Google del propio operador. **No hubo que traer ningun dato:** `AdminSession` y `StaffSession` ya llevan `displayName` y `email` (`apps/web/src/lib/admin-auth.tsx:15-21`, `staff-auth.tsx:11-17`), asi que es presentacion y no plomeria; ninguna llamada nueva, ningun claim nuevo. Hecho: la cabecera pinta el nombre en una linea y `rol - correo` debajo, para las cuatro variantes de rol y tambien para los entrenadores, porque `AdminShell` es la misma para los dos. **El caso borde no es teorico y por eso lleva prueba propia:** `admin-auth` guarda `user.displayName?.trim() ?? ""`, y en produccion hay una cuenta administrativa -la de proveedor `password`- **sin `displayName` ninguno**, verificado con `accounts:lookup` en esta misma sesion. Cuando el nombre falta, el correo ocupa la linea del nombre en vez de dejar un hueco, y no se repite debajo. Del lado del estilo, el correo es texto largo sin espacios donde partir, asi que la pastilla pasa de `flex: 0 0 auto` a `flex: 0 1 auto` con `min-width: 0` y las dos lineas llevan `overflow-wrap: anywhere`: sin eso desbordaria en movil, que es justo lo que vigila el humo e2e. El correo se pinta sin `text-transform: uppercase`, porque un correo no se escribe en mayusculas. Pruebas: dos unitarias nuevas en `apps/web/src/app/admin/page.test.tsx` -identidad completa, y el repliegue al correo cuando no hay nombre, que ademas afirma que no se duplica- y la e2e `qa/tests/admin-auth.spec.ts` deja de afirmar el texto viejo y afirma el nuevo para `owner` y `administrator`. **Las pruebas obligaron a un cambio en el marcado, y conviene decir por que:** el rol se pinta en mas de un sitio -tambien en el pie de la barra lateral- y la pagina tiene mas de un `<header>`, asi que ni preguntar por el texto ni preguntar por el papel `banner` distingue la cabecera. El bloque de identidad lleva `data-testid="admin-identity"`, igual que el propio shell ya llevaba el suyo, y las dos unitarias preguntan dentro de el. **Alcance declarado, para que nadie lo lea de mas:** cubre el turno administrativo y el de entrenador, que comparten `AdminShell`. **El portal de cliente (`/account`) no entra**, no lo pidio el operador y tiene otra cabecera. |
| T027V2 | Overview sin barra de atajos ni conteo de miembros; tres proximos cumpleanos y aviso del dia | - | revision | Pedido por el operador el 2026-09-12. Spec `docs/superpowers/specs/2026-09-12-admin-coach-operations-design.md`, plan `docs/superpowers/plans/2026-09-12-admin-coach-operations.md`. Ventana de cumpleanos a 366 dias. Decision de seguridad (ADR-010): el reporte operativo NO se abre a coaches porque lleva importes; el Overview del coach omite la metrica de membresias vencidas y el enlace a finanzas. Evidencia al cerrar: tests web `overview-page.test.tsx`, Playwright `admin-overview-birthdays.spec.ts`. Verificacion 2026-09-12 sobre be977e6: format:check, lint, typecheck, build, build sintetico, load smoke (240 peticiones, 0 fallos, p95 27 ms) y e2e smoke (6 passed, 2 skipped) en verde; test:rules 13 archivos en verde (contenedor Docker sin red, puerto 8080); test:unit 2658/2659, el unico fallo es member-calendar.test.tsx (fecha real, fuera de esta rama); Playwright de UI 14 passed / 2 skipped; emuladores T093 2 passed y T121 5 passed incl. caso coach. Sin despliegue. Reverificado 2026-09-15 (T039V2): suites web 61 y node 69 en verde; Playwright overview birthdays / attendance roster / enrolment requests / medical references / admin shell 16 passed, 2 skipped en escritorio y movil, con capturas; menu del coach ahora con seis entradas (T037V2). |
| T028V2 | Attendance como lista por clase con etiquetas Ready/Booked/Late y boton Clock in | T027V2 | revision | Cubre la parte "seleccionar de la lista quien esta y quien no" de T014V2; el umbral de tarde pasa a 0 min (`determinePunctuality`). No cubre T015V2 (vencimiento a 20 min): sigue pendiente. Evidencia: `session-roster.test.tsx`, `attendance/page.test.tsx`, Playwright `attendance-roster.spec.ts`. Verificacion 2026-09-12 sobre be977e6: format:check, lint, typecheck, build, build sintetico, load smoke (240 peticiones, 0 fallos, p95 27 ms) y e2e smoke (6 passed, 2 skipped) en verde; test:rules 13 archivos en verde (contenedor Docker sin red, puerto 8080); test:unit 2658/2659, el unico fallo es member-calendar.test.tsx (fecha real, fuera de esta rama); Playwright de UI 14 passed / 2 skipped; emuladores T093 2 passed y T121 5 passed incl. caso coach. Sin despliegue. Reverificado 2026-09-15 (T039V2): suites web 61 y node 69 en verde; Playwright overview birthdays / attendance roster / enrolment requests / medical references / admin shell 16 passed, 2 skipped en escritorio y movil, con capturas; menu del coach ahora con seis entradas (T037V2). |
| T029V2 | Enrolment requests: ayuda por boton, verificacion contra emuladores y acceso del coach a cola y devolucion | T012V2 | revision | Detalle y aprobacion siguen siendo de oficina (ADR-010). Evidencia: `requests/page.test.tsx`, `enrolment-request-callables.test.ts`, spec T121 contra emuladores con caso coach, Playwright `enrolment-requests-ui.spec.ts`. Emuladores 2026-09-12 (contenedor Docker sin red, Firestore 8080): T093 2 passed, T121 5 passed incl. caso coach. Verificacion 2026-09-12 sobre be977e6: format:check, lint, typecheck, build, build sintetico, load smoke (240 peticiones, 0 fallos, p95 27 ms) y e2e smoke (6 passed, 2 skipped) en verde; test:rules 13 archivos en verde (contenedor Docker sin red, puerto 8080); test:unit 2658/2659, el unico fallo es member-calendar.test.tsx (fecha real, fuera de esta rama); Playwright de UI 14 passed / 2 skipped; emuladores T093 2 passed y T121 5 passed incl. caso coach. Sin despliegue. Nota: 22 fallos preexistentes de Playwright fuera de @smoke (admin-auth, staff-management, family-relationships, login-gateway) reproducidos con los cambios apartados; y schedule-security-boundary.test.ts es flaky bajo la suite completa (pasa aislado). Reverificado 2026-09-15 (T039V2): suites web 61 y node 69 en verde; Playwright overview birthdays / attendance roster / enrolment requests / medical references / admin shell 16 passed, 2 skipped en escritorio y movil, con capturas; menu del coach ahora con seis entradas (T037V2). |
| T030V2 | Medical conditions en su ruta con "Show all references" | - | revision | Nuevo callable `listHealthReferences` (nombre + etiqueta, nunca el resumen). Nota: todos los callables de salud exigen `BPT_SYNTHETIC_PILOT=true`; si produccion no lo define, la seccion falla hoy igual que ayer. El guardado del coach pasa por el callable nuevo `saveHealthReferenceLabel`, que solo acepta `studentId` y `staffReferenceLabel` y nunca recibe ni devuelve la nota clinica. Evidencia: `medical/page.test.tsx`, `health-callables.test.ts`, `health-service.test.ts`, Playwright `medical-references.spec.ts`. Verificacion 2026-09-12 sobre be977e6: format:check, lint, typecheck, build, build sintetico, load smoke (240 peticiones, 0 fallos, p95 27 ms) y e2e smoke (6 passed, 2 skipped) en verde; test:rules 13 archivos en verde (contenedor Docker sin red, puerto 8080); test:unit 2658/2659, el unico fallo es member-calendar.test.tsx (fecha real, fuera de esta rama); Playwright de UI 14 passed / 2 skipped; emuladores T093 2 passed y T121 5 passed incl. caso coach. Sin despliegue. Reverificado 2026-09-15 (T039V2): suites web 61 y node 69 en verde; Playwright overview birthdays / attendance roster / enrolment requests / medical references / admin shell 16 passed, 2 skipped en escritorio y movil, con capturas; menu del coach ahora con seis entradas (T037V2). |
| T031V2 | Menu sin Memberships ni Waivers; coach y head coach ven Overview, Attendance, Enrolment requests y Medical conditions | - | revision | ADR-010. Rutas y tests de Memberships/Waivers se conservan. Evidencia: `admin-routes.test.ts`, `page.test.tsx`, Playwright `admin-shell.spec.ts` (caso coach). Verificacion 2026-09-12 sobre be977e6: format:check, lint, typecheck, build, build sintetico, load smoke (240 peticiones, 0 fallos, p95 27 ms) y e2e smoke (6 passed, 2 skipped) en verde; test:rules 13 archivos en verde (contenedor Docker sin red, puerto 8080); test:unit 2658/2659, el unico fallo es member-calendar.test.tsx (fecha real, fuera de esta rama); Playwright de UI 14 passed / 2 skipped; emuladores T093 2 passed y T121 5 passed incl. caso coach. Sin despliegue. Reverificado 2026-09-15 (T039V2): suites web 61 y node 69 en verde; Playwright overview birthdays / attendance roster / enrolment requests / medical references / admin shell 16 passed, 2 skipped en escritorio y movil, con capturas; menu del coach ahora con seis entradas (T037V2). |
| T032V2 | Clases con varios dias por semana, rango de nivel, rango de edad y descripcion (contrato v2, generador, formulario movil) | - | revision | Commits d109663, 11b7de4, a972dca, c1809c8, 976620f (dominio); e58c3c9 (store); e856829, 3569a25 (callables); 4597fa0, 2fc6bf6 (cliente web); cc5b050 (formulario de clase); d1529f6, f8bc1ba (tabla del catalogo). Spec `docs/superpowers/specs/2026-09-14-admin-classes-levels-billing-coach-design.md` (decisiones 1-16), plan `docs/superpowers/plans/2026-09-14-admin-classes-levels-billing-coach.md`. Playwright (Task 22) `--grep "admin classes|admin levels|billing home|admin shell"`: 18 passed / 2 skipped (project-conditional) en desktop-chromium y mobile-chromium, capturas `classes-list-{desktop,mobile}-chromium.png`, `classes-form-{desktop,mobile}-chromium.png`. Emuladores Docker `bpt-emu:local --network none` (demo-bpt-jersey), `run-schedule-finance-e2e.mjs`: `schedule-finance-emulator.spec.ts` 2 passed (clase con dos reglas crea 2 sesiones; conteos en 0; coach denegado; owner edita; removeClass cancela ambas). Task 23 fix re-run PASSED: tras el arreglo 7ef1a3a (seed-auth-emulator.mjs acepta AUTH_EMULATOR_E2E_ACADEMY_ID; el runner ya no genera codigo), run-schedule-finance-e2e.mjs repetido el 2026-09-15 en Docker (bpt-emu:local --network none, emuladores auth+firestore+functions, secretos sinteticos regenerados): schedule-finance-emulator.spec.ts 2 passed (19.4 s), script exit 0 (commits 655dcfe, 7ef1a3a). Nuevos callables a desplegar con el proximo `firebase deploy --only functions:...`: updateSession, removeClass, listSessionBookedCounts, listRecentPayments, getFamilyFinancialAccount, listMemberNames. Desplegado el 2026-09-15 en bptjersey-f5a25 (`firebase deploy --only functions:updateSession,removeClass,listSessionBookedCounts,listRecentPayments,getFamilyFinancialAccount,listMemberNames`, 78 funciones tras el despliegue; via `functions.configDir` temporal con `BPT_WAIVER_REGISTRATION=disabled`, valor que produccion ya llevaba, sin commitear nada). Pendiente de verificacion en produccion antes de pasar a `desplegada`. |
| T033V2 | Editar sesion y eliminar clase (cancela sus sesiones futuras) | T032V2 | revision | Commits a972dca (inputs), e58c3c9 (store: updateSession, removeClass cancela sesiones futuras, salta ids legados), e856829 (callables), bd63aaf (flujos de dialogo). Spec `docs/superpowers/specs/2026-09-14-admin-classes-levels-billing-coach-design.md`, plan homonimo. Playwright (Task 22) `--grep "admin classes|admin levels|billing home|admin shell"`: 18 passed / 2 skipped en desktop-chromium y mobile-chromium, captura `classes-form-{desktop,mobile}-chromium.png`. Emuladores Docker `bpt-emu:local --network none` (demo-bpt-jersey): `schedule-finance-emulator.spec.ts` 2 passed (owner edita sesion, removeClass cancela las 2 sesiones futuras). Task 23 fix re-run PASSED: tras el arreglo 7ef1a3a (seed-auth-emulator.mjs acepta AUTH_EMULATOR_E2E_ACADEMY_ID; el runner ya no genera codigo), run-schedule-finance-e2e.mjs repetido el 2026-09-15 en Docker (bpt-emu:local --network none, emuladores auth+firestore+functions, secretos sinteticos regenerados): schedule-finance-emulator.spec.ts 2 passed (19.4 s), script exit 0 (commits 655dcfe, 7ef1a3a). Nuevos callables a desplegar: updateSession, removeClass. Desplegado el 2026-09-15 en bptjersey-f5a25 (`firebase deploy --only functions:updateSession,removeClass,listSessionBookedCounts,listRecentPayments,getFamilyFinancialAccount,listMemberNames`, 78 funciones tras el despliegue; via `functions.configDir` temporal con `BPT_WAIVER_REGISTRATION=disabled`, valor que produccion ya llevaba, sin commitear nada). Pendiente de verificacion en produccion antes de pasar a `desplegada`. |
| T034V2 | Levels como fichas de cinturon con stripes, filtros por edad y color | - | revision | Commits a7ccb63 (agrupacion), ca74894 (fichas + DESIGN.md §10), 2da2dc4. Spec `docs/superpowers/specs/2026-09-14-admin-classes-levels-billing-coach-design.md`, DESIGN.md §10 (color de cinturon solo en `.belt-bar`/`.belt-tip`/muestras). Playwright (Task 22) `--grep "admin classes|admin levels|billing home|admin shell"`: 18 passed / 2 skipped en desktop-chromium y mobile-chromium, captura `levels-{desktop,mobile}-chromium.png`. Veredictos visuales DESIGN.md: esquinas cuadradas, morado #2F2483 unico acento, colores de cinturon solo en las clases dedicadas, botones >= 3.15rem, una columna en movil, sin scroll horizontal - todo PASA. Desplegado el 2026-09-15 en bptjersey-f5a25 (`firebase deploy --only functions:updateSession,removeClass,listSessionBookedCounts,listRecentPayments,getFamilyFinancialAccount,listMemberNames`, 78 funciones tras el despliegue; via `functions.configDir` temporal con `BPT_WAIVER_REGISTRATION=disabled`, valor que produccion ya llevaba, sin commitear nada). Pendiente de verificacion en produccion antes de pasar a `desplegada`. Arreglo 2026-09-17 (rama `fix/levels-real-catalogue`) con el catalogo real: grados numerados por posicion (`stripeNumber` es null en los 144), bloques de tecnicas por lista distinta con los niveles a los que aplican (165 requisitos visibles), punta de cinturon negra/roja con cintas en `stripeColor` y "+N" por encima de 4 grados. Vitest `corepack pnpm test apps/web/src/app/levels apps/web/src/app/admin/levels apps/web/src/app/coach/levels apps/web/src/app/account/progress`: 22 passed; los 2 tests nuevos de `levels-browser.test.tsx` fallan con el componente anterior. Typecheck web, eslint y prettier en verde. Playwright temporal sobre el export estatico (desktop y mobile): 27 fichas, 3 bloques `Techniques · Belt, 1st–4th stripe`, +7 x4 y +4 x6, cintas blancas, punta roja solo en BLACK BELT. |
| T035V2 | Billing como home financiero: 20 ultimos pagos, historial por miembro, operativa plegada | T036V2 | revision | Commits 1658e00, 30e887e; redireccion de finanzas `/admin/finance -> /admin/billing`. Task 20 componentes: `apps/web/src/app/admin/billing` 7 archivos / 25 tests. Playwright (Task 22) `--grep "admin classes|admin levels|billing home|admin shell"`: 18 passed / 2 skipped; re-ejecucion de billing home tras el fix del leak-regex (commit 3a67571): 8 passed, capturas `billing-home-{desktop,mobile}-chromium.png`. Emuladores Docker `bpt-emu:local --network none` (demo-bpt-jersey): `schedule-finance-emulator.spec.ts` 2 passed (factura sin membresia, pago en efectivo, ultimos pagos, cuenta financiera de familia, nombres de miembro). Task 23 fix re-run PASSED: tras el arreglo 7ef1a3a (seed-auth-emulator.mjs acepta AUTH_EMULATOR_E2E_ACADEMY_ID; el runner ya no genera codigo), run-schedule-finance-e2e.mjs repetido el 2026-09-15 en Docker (bpt-emu:local --network none, emuladores auth+firestore+functions, secretos sinteticos regenerados): schedule-finance-emulator.spec.ts 2 passed (19.4 s), script exit 0 (commits 655dcfe, 7ef1a3a). Nuevos callables a desplegar: listRecentPayments, getFamilyFinancialAccount, listMemberNames. Desplegado el 2026-09-15 en bptjersey-f5a25 (`firebase deploy --only functions:updateSession,removeClass,listSessionBookedCounts,listRecentPayments,getFamilyFinancialAccount,listMemberNames`, 78 funciones tras el despliegue; via `functions.configDir` temporal con `BPT_WAIVER_REGISTRATION=disabled`, valor que produccion ya llevaba, sin commitear nada). Pendiente de verificacion en produccion antes de pasar a `desplegada`. |
| T036V2 | Issue invoice con busqueda de miembro y membresia opcional; Record payment efectivo/transferencia | - | revision | Commits ec804e0, 755e0b7, a432bad, df52120 (backend); 63005fa (clientes); 53d125f, 4303da0, faa1099 (selector de miembro); 67a87b0, 80b89ec (dialogos). Decision 16 del spec: Cash / Bank transfer unicamente en la UI. Playwright (Task 22) `--grep "admin classes|admin levels|billing home|admin shell"`: 18 passed / 2 skipped, captura `billing-invoice-{desktop,mobile}-chromium.png`. Emuladores Docker `bpt-emu:local --network none` (demo-bpt-jersey): `schedule-finance-emulator.spec.ts` 2 passed (factura sin membresia -> pago en efectivo -> pagos recientes -> cuenta de familia -> nombres de miembro; coach denegado). Task 23 fix re-run PASSED: tras el arreglo 7ef1a3a (seed-auth-emulator.mjs acepta AUTH_EMULATOR_E2E_ACADEMY_ID; el runner ya no genera codigo), run-schedule-finance-e2e.mjs repetido el 2026-09-15 en Docker (bpt-emu:local --network none, emuladores auth+firestore+functions, secretos sinteticos regenerados): schedule-finance-emulator.spec.ts 2 passed (19.4 s), script exit 0 (commits 655dcfe, 7ef1a3a). Nuevos callables a desplegar: listRecentPayments, getFamilyFinancialAccount, listMemberNames. Desplegado el 2026-09-15 en bptjersey-f5a25 (`firebase deploy --only functions:updateSession,removeClass,listSessionBookedCounts,listRecentPayments,getFamilyFinancialAccount,listMemberNames`, 78 funciones tras el despliegue; via `functions.configDir` temporal con `BPT_WAIVER_REGISTRATION=disabled`, valor que produccion ya llevaba, sin commitear nada). Pendiente de verificacion en produccion antes de pasar a `desplegada`. |
| T037V2 | Coach y head coach ven Classes (coach en lectura) y Levels | T031V2 | revision | Commit 10debfd (admin-routes, enmienda ADR-010 2026-09-14), d1529f6 (`canManage = role !== "coach"`). ADR-010 "Enmienda 2026-09-14" (coach lee Classes, ve Levels). Playwright (Task 24) `--grep "admin overview birthdays|attendance roster|enrolment requests|medical references|admin shell"`: 16 passed / 2 skipped en desktop y movil, captura `admin-shell-coach-desktop-chromium.png`. Desplegado el 2026-09-15 en bptjersey-f5a25 (`firebase deploy --only functions:updateSession,removeClass,listSessionBookedCounts,listRecentPayments,getFamilyFinancialAccount,listMemberNames`, 78 funciones tras el despliegue; via `functions.configDir` temporal con `BPT_WAIVER_REGISTRATION=disabled`, valor que produccion ya llevaba, sin commitear nada). Pendiente de verificacion en produccion antes de pasar a `desplegada`. |
| T038V2 | Calendario de miembros conectado: conteo de reservas real y ficha con nivel, edad y descripcion | T032V2 | revision | Commits 4597fa0 (cliente), 6d1e01e (adaptador + tarjeta + fixtures + nota en STACK.md sobre `NEXT_PUBLIC_CALENDAR_SOURCE=firebase`). Task 21 calendario: `apps/web/src/lib/calendar` + `apps/web/src/app/account` 67/67. Caveat: `/account` no se puede probar en navegador contra emuladores (App Check fail-closed); verificado por unit + component + suites de callables contra emuladores (`schedule-finance-emulator.spec.ts` 2 passed, incluye `listSessionBookedCounts`); ultima verificacion en staging con `NEXT_PUBLIC_CALENDAR_SOURCE=firebase` pendiente del operador. Desplegado el 2026-09-15 en bptjersey-f5a25 (`firebase deploy --only functions:updateSession,removeClass,listSessionBookedCounts,listRecentPayments,getFamilyFinancialAccount,listMemberNames`, 78 funciones tras el despliegue; via `functions.configDir` temporal con `BPT_WAIVER_REGISTRATION=disabled`, valor que produccion ya llevaba, sin commitear nada). Pendiente de verificacion en produccion antes de pasar a `desplegada`. |
| T039V2 | Verificacion de los cinco puntos de coaches (T027V2-T031V2) con capturas y suites | T027V2 | revision | Commit 05020d6 (capturas en las cinco specs). Suites web (Task 24 paso 1): overview-page, attendance, members/requests, members/medical, admin-routes, admin page: 7 archivos / 61 passed; suites node: birthdays, pre-class contracts, health, enrolment-request callables: 5 archivos / 69 passed. Playwright (Task 24) `--grep "admin overview birthdays|attendance roster|enrolment requests|medical references|admin shell"`: 16 passed / 2 skipped en desktop-chromium y mobile-chromium, capturas `overview-birthdays-{desktop,mobile}-chromium.png`, `attendance-roster-{desktop,mobile}-chromium.png`, `enrolment-requests-office-{desktop,mobile}-chromium.png`, `enrolment-requests-coach-{desktop,mobile}-chromium.png`, `medical-references-{desktop,mobile}-chromium.png`, `admin-shell-coach-desktop-chromium.png`. Observacion preexistente, fuera de alcance: la etiqueta de nombre lima de la banda de cumpleanos (T027V2). Sin despliegue. Despliegue A del 2026-09-15 tras la prueba del operador en produccion: `/admin/classes` fallaba por CORS porque `listScheduleCatalog` nunca se habia desplegado (404 sin cabeceras); se desplegaron 39 callables que la web ya llamaba y nunca existieron en produccion (catalogo/clases/reservas, attendance, referencias medicas, billing, calendario de miembros) y se actualizaron 22 de modulos cambiados en esta rama (incluida `listUpcomingBirthdays`, que devolvia 400 por version antigua). Produccion pasa de 78 a 117 funciones; `selfCheckIn` (otra sesion) excluida. |

## V2-F - Pagos, suscripciones y centro

| ID   | Tarea atomica | Depende de | Estado | Evidencia de salida |
| ---- | ------------- | ---------- | ------ | ------------------- |
| T018V2 | Bloquear la reserva a quien no se le cobro la mensualidad | - | pendiente | **Hallazgo de la auditoria previa:** la regla ya esta escrita y no la usa nadie. `evaluateBookingEligibility` (`packages/domain/src/schedule/schedule-contracts.ts:911-953`) ya rechaza a quien no esta `active` o `trial` -es decir, ya cubre `overdue`-, pero una busqueda en `apps/` no encuentra **ni una sola llamada** a esa funcion. La regla existe en el dominio y no se aplica en ningun callable. La fila tiene dos mitades: cablear la funcion en la ruta real de reserva, y garantizar que un cobro fallido transiciona de verdad la membresia a `overdue`. Sin la segunda mitad, la primera no bloquea a nadie. **Verificado por los dos lados el 2026-09-09:** la segunda mitad de la fila esta entera por construir. Lo unico que transiciona una membresia a `overdue` es un administrador pulsando «Mark overdue» (`apps/functions/src/memberships/membership-callables.ts:563-565`); ni finance, ni payments, ni penalties llaman a `transitionMembership`. Y el `overdue` del panel financiero es **otro concepto**: un flag por factura calculado al vuelo (`packages/domain/src/finance/financial-dashboard.ts:126`), no persistido y sin relacion con el estado de la membresia. |
| T019V2 | Etiquetar cada pago con el centro al que pertenece y elegir centro de retirada del merchandising | T023V2 | pendiente | Cada pago lleva en su referencia si es de Town o de West. En la compra de merchandising el comprador elige donde retirarla, y esa eleccion es la que etiqueta el pago. Verificado que no existe hoy: no hay concepto de retirada ni de centro en `packages/domain/src/shop`. Es construccion nueva, no ajuste. **Ampliado el 2026-09-09:** el pedido tampoco tiene referencia de pago, porque no hay pago online -`shopPaymentMethodNote` dice que se cobra en la academia al retirar-, asi que etiquetar el pago no es anadir un campo a algo que ya existe. Del lado de finanzas, `invoiceReference` no se genera: lo teclea la oficina en el formulario de facturacion y solo se valida y se usa como clave de idempotencia. Y centro esta dicho de ocho maneras distintas en el repositorio, con dos mayusculas incompatibles: `locationIds = ["town", "west"]` en horarios frente a `siteValues`, `trainingCenters` y `upcomingBirthdayTrainingCenters` en `["Town", "West"]`, mas literales en linea en familias y en el informe operativo, mas el `trainingCenter` de texto libre de la importacion de Regyfit. Ya hay una traduccion a mano entre las dos (`booking-transaction-service.ts:679`). Etiquetar pagos por centro sin unificar antes ese vocabulario reparte el problema en vez de resolverlo. **Interrogada y decidida el 2026-09-09, y la fila resulta ser dos problemas distintos.** Las suscripciones **si** tienen a que agarrarse: `invoices` y `payments` son documentos persistidos (`apps/functions/src/finance/finance-service.ts:222-236`), con `chargeKind: membership | payg_session | manual_adjustment`. El merchandising **no**: un pedido lleva un `paymentStatus: "unpaid" | "paid"` dentro del propio pedido (`packages/domain/src/shop/shop-contracts.ts:31`) y no genera ni factura ni pago. Decidido: la etiqueta de una suscripcion sale del **`trainingCenter` del miembro**, tambien en los planes que cruzan sedes -«BPT Jersey Adult» cubre las dos y «West Kids 2x» incluye Open Mats de Town-, donde el centro no es un hecho sino una convencion. El centro es **campo propio de la factura**, no un prefijo dentro de `invoiceReference`: esa referencia la teclea la oficina, se valida con una regex permisiva y es la **clave de idempotencia** de la emision, comparada por consulta dentro de la transaccion (`finance-service.ts:649-711`), asi que imponerle formato seria tocar la idempotencia de la facturacion para ganar legibilidad. El merchandising lleva el **centro de retirada en el propio pedido**, sin inventar una factura que hoy no existe: `shopPaymentMethodNote` dice que se paga en la academia al retirar, de modo que el centro de retirada ya es el del cobro. Y la unificacion del vocabulario **sale de esta fila**: es T023V2, de la que esta depende. |
| T020V2 | Confirmar que la base de miembros muestra el centro donde entrena cada miembro | - | pendiente | **Auditado antes de abrir la fila: ya existe.** La columna "Training center" esta definida en `apps/web/src/app/admin/members/page.tsx:37-41` y `trainingCenter` es campo persistido con valores Town/West en `apps/functions/src/profiles/profile-service.ts`. La fila se reduce a comprobar con datos reales que la columna se puebla para todos los miembros y no queda vacia en los importados; si se puebla, se cierra como verificada sin escribir codigo. |
| T023V2 | Un unico conversor canonico de centro, sin migrar datos | - | pendiente | Sale de la ronda del 2026-09-09 y bloquea a T019V2. «Centro» esta dicho de ocho maneras con dos mayusculas incompatibles, y **tres de las capitalizadas estan escritas en Firestore**: `trainingCenter` en estudiantes (`apps/functions/src/profiles/profile-service.ts:318,841`), en el directorio de miembros (`packages/domain/src/members/member-directory-contracts.ts:310,403,428`) y en familias (`packages/domain/src/families/family-contracts.ts:374`), mas `classSites`/`openMatSites` en los planes (`packages/domain/src/memberships/plan-contracts.ts:37,287-290`), mas el `trainingCenter` de texto libre de la importacion de Regyfit, cuyos valores son arbitrarios y no se arreglan con un `toLowerCase()`. **Decidido: no se migra.** La fila construye una unica conversion en el dominio que sustituya las cinco ternarias escritas a mano y las dos comparaciones literales, y que falle a la vista ante un valor inesperado. Ese es el bug real que hay debajo: `apps/functions/src/schedule/booking-transaction-service.ts:679` hace `locationId === "town" ? "Town" : "West"`, de modo que un tercer valor caeria **silenciosamente** en West. Los identificadores en minuscula ya son canonicos en horarios y no necesitan migracion. La unificacion completa -mas de sesenta ficheros con pruebas y migracion de datos ya escritos- queda expresamente **fuera de alcance**, y se registra aqui para que nadie la reabra por sorpresa. |

## V2-G - Datos que faltan del operador

| ID   | Tarea atomica | Depende de | Estado | Evidencia de salida |
| ---- | ------------- | ---------- | ------ | ------------------- |
| T021V2 | Recoger los datos que solo el operador tiene | - | bloqueada | Bloquea T010V2 y T011V2 y no puede resolverse desde el codigo. Faltan: (a) el domicilio completo del segundo centro, BPT West / Strive, con codigo postal; (b) la credencial o grado de Amone Mouton, Catalina Bruma y Connor Hoopes, uno por instructor; (c) confirmacion explicita de si Eduardo "Eddie" Afonso y Andrew "Topo" Toporis se retiran de la landing o solo dejan de aparecer en ese bloque. Ninguno de los tres se rellena por suposicion. |

## V2-H - Check-in del propio miembro

| ID   | Tarea atomica | Depende de | Estado | Evidencia de salida |
| ---- | ------------- | ---------- | ------ | ------------------- |
| T040V2 | Slider "Ready for Jiu Jitsu" en /account: check-in del propio miembro con puerta de 50 m en el servidor y puntualidad | - | aprobada | Spec `docs/superpowers/specs/2026-09-14-ready-for-jiu-jitsu-self-check-in-design.md` (20 decisiones, grill 2026-09-15) y plan `docs/superpowers/plans/2026-09-15-ready-for-jiu-jitsu-self-check-in.md`. Construido: reglas puras en `packages/domain/src/schedule/self-check-in-contracts.ts` (ventana -60/+20 min, open mat hasta `endAt`, candidato, parser estricto, puerta distancia ≤ 50 m con precision ≤ 100 m); callable `selfCheckIn` solo para adultStudent/teenStudent/guardian, transaccion `recordSelfCheckIn` que juzga en el servidor y nunca persiste coordenadas (prueba de fuga en `schedule-security-boundary.test.ts`); metodo `self`; rol `teenStudent` conocido por el servidor; tarjeta `ready-for-jiu-jitsu.tsx` primera en `/account`, sondeo cada 60 s (costura con el equipo de coaches: cualquier asistencia esconde el slider). Fixture con los pines de Town y West; adaptador Firebase escrito. Evidencia: vitest dominio+functions+web en verde, Playwright `account-self-check-in.spec.ts` 8/8 en :9471 con geolocalizacion real, capturas `qa/screenshots/ready-*.png`; emulador en contenedor aislado con Java 21 (commit `94ac8c5`): **3/3 pruebas en verde**, incluida la de `selfCheckIn` (rechazo fuera del radio con 120 m, asistencia `self`/`attended`, replay identico, rechazo del owner, sin coordenadas en record ni error). **Revalidacion del 2026-09-15 (noche) sobre `c507c92`**: Playwright `account-self-check-in.spec.ts` **8/8** en :9471 (desktop + mobile: alias `/accounts`, End, persistencia tras recarga, ArrowRight < 95 y commit al 95, gesto duplicado ignorado, 120 m rechazado, permiso denegado, tutor cambia al hermano listo) y las 8 capturas `qa/screenshots/ready-{idle,locating,done,refused}-{phone,desktop}.png` renovadas e inspeccionadas contra DESIGN.md §10. **Distincion honesta**: lo probado en emulador es la callable; el adaptador Firebase del navegador (`firebase-calendar-repository.ts`) sigue sin ejecutarse contra Firebase porque App Check es fail-closed fuera de produccion. APROBADA. Pendiente solo para `desplegada`: desplegar `functions:selfCheckIn`, cargar los pines en `/admin/classes` (hasta entonces responde `site_not_ready`) y verificar el flujo completo con una sesion real. |

## V2-I - Racha, competidores y ajustes de la cuenta

| ID   | Tarea atomica | Depende de | Estado | Evidencia de salida |
| ---- | ------------- | ---------- | ------ | ------------------- |
| T041V2 | Fase 0 de la interfaz de miembros: contratos compartidos, huecos en /account y ficheros reservados por equipo para que racha, competidores y ajustes se construyan en paralelo | - | aprobada | Spec `docs/superpowers/specs/2026-09-16-member-engagement-phase-0-design.md` (10 decisiones, tabla de propiedad de ficheros por equipo y orden de subida Ajustes -> Racha -> Competidores) y plan `docs/superpowers/plans/2026-09-16-member-engagement-phase-0.md`. Construido el 2026-09-16: modulo puro `@bpt-jersey/domain/members/engagement` (racha por sesiones con hueco maximo de 7 dias, inicio de temporada = 1 de septiembre en Europe/Jersey, horas y asistencias desde esa fecha, barras de meta y recompensa con `almost` a falta de una, ficha publica `MemberPublicCard`, `compareTechniques`, `rankNeighbours`) con 13 pruebas; `photoUrl` https opcional en `StudentProfile` (2 pruebas nuevas, 213 del paquete en verde); tres ficheros de callables vacios ya reexportados desde `apps/functions/src/index.ts`; `topSlot` en `MemberCalendar` entre el slider y la cabecera, enlaces Progress/Competitors/Settings en la cabecera, rutas `/account/competitors` y `/account/settings` reservadas, `StreakPanel` vacio montado, `apps/web/public/animations/streak-flame.json` y `lottie-web` 5.13.0 (86 pruebas web de /account en verde; `pnpm audit` no anade hallazgos: los 6 altos y 2 criticos existentes son de next, sharp, fast-uri y js-yaml). Sin cambios en `firestore.rules` ni en indices por decision 4. `STACK.md` y `BRIEF.md` retirados en la misma serie. Pendiente para `desplegada`: subir la serie a `main` (lo hace el operador, el agente tiene el push bloqueado) y ver el despliegue de Cloudflare Pages. |
| T042V2 | Racha y progreso en lo alto de /account: titulo Streak, barra de la proxima meta y de la proxima recompensa (tono mas intenso y «Just x1 missing to get goal/reward!» a falta de una), llama animada con multiplicador x2, x3... y horas de entrenamiento desde septiembre; avisos de la proxima promocion segun asistencia | T041V2 | pendiente | Rellena `apps/web/src/app/account/streak/streak-panel.tsx`, que la fase 0 ya monta en el `topSlot` de `MemberCalendar`. Los numeros salen de `buildMemberStreakSummary` (`@bpt-jersey/domain/members/engagement`) alimentado por un callable propio en `apps/functions/src/streak/streak-callables.ts` que lea las asistencias del estudiante y la duracion de cada sesion; la llama es `/animations/streak-flame.json` con `lottie-web` (respetar `prefers-reduced-motion`). Quien configura meta y recompensa lo decide esta fila; los valores por defecto son 10 y 25 asistencias de temporada. No toca `page.tsx`, `member-calendar.tsx`, `profile-contracts.ts`, reglas ni indices. |
| T043V2 | Competidores en /account/competitors: dos tablas (asistencia y progresion de cinturon) donde el miembro ve a los dos mas cercanos por encima y por debajo, con foto, nombre, cinturon y grados, racha de asistencia y las tecnicas que cada uno tiene y el otro no | T044V2 | pendiente | Rellena `apps/web/src/app/account/competitors/page.tsx` (ruta y enlace ya reservados). Datos por callable propio en `apps/functions/src/competitors/competitors-callables.ts` que devuelva `MemberPublicCard[]` (decision 4: sin lectura directa de Firestore, sin cambios de reglas ni indices); `rankNeighbours` y `compareTechniques` ya existen en `@bpt-jersey/domain/members/engagement`; la progresion de cinturon reutiliza `buildPeerComparison` de levels. **Cohortes por edad (decision 5, operador 2026-09-16):** dos cohortes separadas calculadas en el servidor con `leaderboardCohort(dateOfBirth, now)` de `members/engagement`: los menores de 16 solo ven y son vistos por menores de 16; desde los 16 se tratan como adultos en las tablas. La DPIA de T011 debe recogerlo; no bloquea. Depende de T044V2 por la foto (`photoUrl`). |
| T044V2 | Ajustes de cuenta en /account/settings: el adulto cambia cualquier dato; el menor solo su foto y el tutor la aprueba; el tutor da acceso al adolescente con usuario y contrasena o con su cuenta de Google | T009V2 | pendiente | Rellena `apps/web/src/app/account/settings/page.tsx` (ruta y enlace ya reservados). La foto se guarda en `StudentProfile.photoUrl` (https, <= 512 caracteres, ya validado en `profile-contracts.ts`) a traves de `apps/functions/src/profiles/profile-service.ts` y el almacenamiento privado de R2 (ADR-003); la aprobacion del tutor es un estado propio de esta fila. El acceso del adolescente reutiliza el rol `teenStudent` y `canonical-client-student-scope.ts` y se apoya en T009V2 (DPIA y retencion de T011 incluidas); habilitar Google como proveedor en Firebase Auth de produccion es configuracion del operador. Sube a `main` antes que T042V2 y T043V2 porque cambia roles. |

---

## V2-J - Clon de Classes / Services de Regyfit

| ID   | Tarea atomica | Depende de | Estado | Evidencia de salida |
| ---- | ------------- | ---------- | ------ | ------------------- |
| T045V2 | Captura de solo lectura de las nueve subsecciones de Classes / Services de Regyfit (estructura, subpestañas, valores de configuracion, clases programadas e historial) | - | aprobada | Capturado el 2026-09-16 con Chrome visible por noVNC en la Tailnet y salida por tunel SSH inverso desde el portatil del operador (Regyfit bloquea la IP del VPS). Inventario saneado en `docs/data/migrations/regyfit/classes-services-inventory.md` (sin nombres ni IPs); crudo (25 MB de capturas y HTML, 2 sedes, 31 tipos, 10 planes, 1 descuento, 2 packs, opciones, drop-in, 177 clases sep-oct y 1000 filas de historial) en `/root/regyfit-capture/` fuera del repo, con los scripts CDP en `/root/regyfit-capture/scripts/`. Spec de diseno `docs/superpowers/specs/2026-09-16-regyfit-classes-services-clone-design.md` (4 decisiones de alcance + 14 de diseno, aprobadas en chat el 2026-09-16). |
| T046V2 | Plan 1: cascara `/admin/classes-services` con nueve pestañas + Locations dinamicas + Class / Service Types (programas v2) + Classes & Services 2.0 (calendario, lista, panel de sesion, inscripciones, copy/delete week); sustituye `/admin/classes` | T045V2 | revision | Plan en `docs/superpowers/plans/2026-09-16-classes-services-plan-1-shell-locations-types-classes.md`, ejecutado por subagentes (implementador + revisor + rondas de fix) en la rama `feature/classes-services-clone`. **Estado 2026-09-16: Tareas 1-12 de 12 completas.** Tareas 1-6 (dominio, store, callables, cliente web, cascara de nueve pestañas). Tarea 7 Locations fusionada por Equipo A (`e0a13b6`), Tarea 8 Types fusionada por Equipo B (`ca45081`), Tareas 9-10 Classes & Services 2.0 (calendario, lista, panel de sesion, inscripciones, copiar/eliminar semana) fusionadas por Equipo C (`146cf2f`), Tarea 11 reglas de Firestore fusionada por Equipo D (`b30941b`); enmienda de ADR-010 que abre `listStaffProfiles` en lectura a `headCoach` en `e8798a5`. Tarea 12 (esta fila): spec Playwright `qa/tests/admin-classes-services.spec.ts` (locations crear/alternar estado, types color/drop-in en linea, calendario semanal + panel de sesion + vista previa de copiar semana, coach con tres pestañas sin mutaciones) en verde en `desktop-chromium` y `mobile-chromium` (8/8), con capturas `qa/screenshots/cs-locations.png`, `cs-types.png`, `cs-classes-week.png` y sus pares `-phone`. `corepack pnpm verify:mvp` en verde hasta `test:unit` (format:check, lint, typecheck, build, test:unit: 323 ficheros / 2964 pruebas) y se detiene en `test:rules` por falta de Java en este host; `test:rules` corrido aparte en el contenedor de emuladores `bpt-emu:local` (13 ficheros / 93 pruebas, verde). Los tres pasos restantes de `verify:mvp` corridos a mano con `NEXT_PUBLIC_ADMIN_E2E=true`: `build` estatico verde, `test:load:synthetic` verde (240 peticiones, 0 fallos), `test:e2e:smoke` con 2 fallos en `public-home.spec.ts` (precios de la portada) ajenos a este plan — vienen de cambios sin commitear en `apps/web/src/content/academy.ts` que ya estaban en el checkout antes de esta tarea, fuera de alcance. **Pendiente:** subir la rama a `main` (push/PR) queda para el operador; revision final de rama. **2026-09-17 rediseño de la pestaña Types** (estilo SaaS de Regyfit, sin dependencias nuevas): interruptores `role="switch"`, badge de color con tinta por contraste WCAG, orden por nombre, edición en línea de nombre/abreviatura vía `updateProgram`, candado si está en uso y papelera desactivada (no existe callable de borrado). Evidencia: `page.test.tsx` 11/11, `admin-classes-services.spec.ts` 8/8 en `desktop-chromium` y `mobile-chromium` con build `NEXT_PUBLIC_ADMIN_E2E=true`, capturas `qa/screenshots/cs-types.png` y `-phone`, typecheck y lint web en verde. |
| T047V2 | Plan 2: Memberships and Vouchers (planes dinamicos, descuentos, packs de creditos) + Drop-ins + Bulk Operations; sustituye `/admin/memberships` | T046V2 | pendiente | Plan en `docs/superpowers/plans/` (por escribir). |
| T048V2 | Plan 3: Listings & Reports (8 informes XLSX/PDF imprimible) + Options (documento de configuracion) + History (auditoria con IP) | T047V2 | pendiente | Plan en `docs/superpowers/plans/` (por escribir). Sub-tramo History completado como T048V2-H (fila siguiente); Listings & Reports y Options siguen pendientes de plan. |
| T048V2-H | Sub-tramo "History" de T048V2: registro auditado de reservas/cancelaciones/asistencia con IP del actor, filtros (fecha, persona, tipo de registro), exportacion a PDF y purga automatica de IPs a los doce meses | T048V2 | desplegada | Spec `docs/superpowers/specs/2026-09-17-classes-services-history-design.md`, plan de 15 tareas TDD en `docs/superpowers/plans/2026-09-17-classes-services-history.md`, rama `feature/cs-history` (worktree `/root/BPT-Jersey-wt/history`), commits `5a2b881..8497caa` (21 commits, tareas 1-13 con revision limpia por subagente revisor tras cada ronda; ver `.superpowers/sdd/2026-09-17-classes-services-history/progress.md`). Entrega: eventos de auditoria de clase con `actorRole`/`actorGroup`/IP (`packages/domain/src/audit`), IP del llamador leida de la cadena `x-forwarded-for` y grabada en la misma transaccion de `bookClass`/`cancelBooking` y las cuatro callables de asistencia, callables `listClassHistory` y `exportClassHistoryPdf` (solo owner ve la columna IP; administrator no, por minimo privilegio), pantalla `/admin/classes-services/history` (filtros Since/Since time/Logged by/Registration type/No. of records, tabla DATE/TIME·USER·IP·TASK en orden mas-reciente-primero, boton PDF), y sweep programado diario `sweepClassIpRetentionSchedule` que borra `actorIp` de cualquier evento de auditoria (no solo de clase) con mas de doce meses. Evidencia real de esta tarea (gate T14, 2026-09-18, sobre `8497caa`, sin tocar produccion): `format:check` verde; `lint` 0 avisos; `typecheck` verde en los 6 workspaces; `build` (`tsc` functions + `next build` web) verde, incluye la ruta `/admin/classes-services/history`; `test` (unitarias web+node) **338 ficheros / 3184 pruebas verdes**; `test:rules` no pudo arrancar con el comando plano (puerto 8080 lo ocupa code-server en este host) y se corrio en el contenedor `bpt-emu:local` (`--network none`, JDK 21, proyecto `demo-bpt-jersey`): **13 ficheros / 94 pruebas verdes**, incluye la denegacion total de `auditEvents` a cualquier cliente (T12); build con `NEXT_PUBLIC_ADMIN_E2E=true` verde; `test:load:synthetic` verde (240 peticiones, 0 fallos); `test:e2e:smoke` con ese build: 2 verdes, 4 rojas, 2 omitidas, reproducidas dos veces en esta sesion (`git status --short` vacio en este worktree; `apps/web/src/content/academy.ts` identico a `main`, sin cambios sin commitear aqui). Causa establecida leyendo el `error-context.md` de cada fallo: `admin-shell.spec.ts` — `getDailyOperationsDashboard`/`getOperationalReport` estan interceptadas por `installAdminFixture`, pero sin `NEXT_PUBLIC_FIREBASE_APP_CHECK_SITE_KEY` ni token de depuracion `initializeFirebaseAppCheck` (`apps/web/src/lib/firebase-client.ts`) lanza antes de llamar a la funcion, así que la pagina renderiza la alerta "Unable to load today's connected dashboard" en vez del estado vacio que la prueba espera; `public-home.spec.ts` — la direccion "Office 9, 13 Library Place" aparece dos veces en la portada (secciones "Train in Jersey" y "Training centres"), violacion de modo estricto, desde el commit `4808277` ("landing with Town and West centres..."), que es antecesor de esta rama (`git merge-base --is-ancestor 4808277 HEAD` confirma). Ambas causas son preexistentes al plan de History, no tocan `classes-services/history` y coinciden con lo ya anotado como ajeno en la fila T050V2. Prueba de despliegue de functions (T14 paso 2): `corepack pnpm --filter @bpt-jersey/domain build:runtime` y `corepack pnpm --filter @bpt-jersey/functions build` verdes sobre el dominio compilado (`deploy-runtime.ts` ya mapea `@bpt-jersey/domain/audit/class-history`), sin errores de mapeo. Ademas, ya registrado en tareas previas de este plan (citado, no re-ejecutado en este gate): Playwright real contra emuladores (`qa/tests/admin-classes-services-history.spec.ts`, `T048_CLASS_HISTORY_EMULATOR_E2E=true` en `bpt-emu-pw:local`) **2 passed** dos veces, con prueba de falsacion (falla si se desactiva el seed); capturas `qa/screenshots/cs-history-desktop.png` y `-mobile.png` comparadas contra la captura de referencia de Regyfit. **Cambios en la superficie de despliegue, ninguno desplegado todavia:** callables nuevas `listClassHistory` y `exportClassHistoryPdf`; funcion programada nueva `sweepClassIpRetentionSchedule`; callables de `schedule` modificadas para grabar la IP del actor — `bookClass`, `cancelBooking` y las cuatro callables de asistencia; indice compuesto nuevo en `firestore.indexes.json` sobre `auditEvents` (`action` ASC, `actorGroup` ASC, `occurredAt` DESC). **Pendiente: el despliegue a produccion requiere confirmacion explicita del operador en chat (no se desplego nada en esta tarea; ningun comando toco `firebase deploy` ni un remoto).** Tras el "si" del operador, desplegar web y functions juntos y verificar `listClassHistory` con `curl -X POST` esperando `401` (no `404`). **Cierre 2026-09-18 (despliegue e importacion reales, autorizados por el operador en chat):** rama fusionada en `main` (`f76747d`, empujada a origin; Cloudflare Pages republica la web). Gate sobre el arbol exacto publicado, en `/root/BPT-Jersey-wt/merge-main`: `typecheck` verde, `lint` 0 avisos, `format:check` verde, `test` **339 ficheros / 3258 pruebas verdes** (los 6 ficheros `Lista*/Listav2*` fallaban solo por sparse-checkout; se materializaron con `git sparse-checkout add /Lista /Listav2` y pasaron). Desplegadas **18 functions** en `bptjersey-f5a25`, todas `Successful update operation` (`listClassHistory`, `exportClassHistoryPdf`, `sweepClassIpRetentionSchedule` y las 15 que ahora graban la fila de auditoria). El despliegue fallo dos veces con `In non-interactive mode but have no value for the following environment variables: BPT_WAIVER_REGISTRATION`: el worktree recien creado no tenia `.env`; el fichero vive fuera del repo en `/root/BPT-Jersey/.tmp/functions-env/.env` y hay que copiarlo a `apps/functions/.env` (gitignored) — desde ahi lo arrastra `pnpm deploy` al artefacto; ponerlo en `.firebase-functions/.env` NO sirve porque el predeploy regenera ese directorio. **Importacion del historial de Regyfit ejecutada con `--apply --i-know-this-is-production`:** de 3735 filas capturadas, 3689 escritas (0 saltadas), 46 rechazadas por frase no reconocida. Verificado leyendo Firestore, no solo el informe del script: 3689 documentos con `actorId == "regyfit-import"`, **2607 con `class.memberId`** (90,3% de las 2889 filas que nombran persona; 92,6% excluyendo drop-ins, que por definicion no son socios) y **1511 con `class.sessionId`**. Las 2297 filas sin resolver quedan en `/root/regyfit-capture/data/history-unmatched.json` (fuera del repo: lleva nombres reales). La via de emparejar por numero de socio no se activo nunca — la columna de referencia de Regyfit viene vacia en las 3735 filas — asi que todo el emparejamiento lo hizo el nombre normalizado; la rama "nombre ambiguo" solo esta probada por unit test. Clave de cuenta de servicio borrada del VPS con `shred -u` (sin copias residuales); **queda pendiente que el operador la revoque en la consola de Firebase**. |
| T049V2 | Plan 4: importacion desde Regyfit (catalogo, sesiones, inscripciones, drop-ins, historial) bajo el runbook de staging privado; emulador primero, produccion solo con confirmacion en chat | T048V2 | pendiente | **Plan 4a (tipos y sesiones) en `docs/superpowers/plans/2026-09-17-classes-services-plan-4a-import-types-sessions.md`**, rama `feature/cs-p4a-import`, commits `4dcc667..5043fb0`: mapeador puro `qa/scripts/regyfit-classes-services-map.mjs` y CLI `qa/scripts/regyfit-classes-services-import.mjs` (dry run por defecto; emulador o produccion con confirmacion explicita). Pruebas unitarias `qa/unit/regyfit-classes-services-map.test.ts` 20/20 con datos sinteticos (incluye: el destino emulador exige un project id `demo-`, y dos tipos que dan el mismo `programId` abortan el plan). Spec de emulador `qa/tests/classes-services-import-emulator.spec.ts` (runner `qa/scripts/run-classes-services-import-ui-e2e.mjs`, fixture sintetico) `1 passed` el 2026-09-17 en `desktop-chromium`, con prueba negativa (sin aplicar la importacion el contador Classes da 0 y la spec falla). La aplicacion es solo de creacion (lee los documentos existentes y solo crea los ausentes; una re-ejecucion nunca modifica documentos existentes): en un mismo `emulators:exec` con el fixture sintetico, primera ejecucion `created 7 / skippedExisting 0`, segunda `created 0 / skippedExisting 7`, y despues la spec `1 passed` (2026-09-17). Ensayo con la semana real 14-20 sep en un emulador desechable, instante fijado 2026-09-17T12:00Z: 26 sesiones / 31 tipos / 5 filas duplicadas descartadas, 17 `completed` + 9 `scheduled`; el calendario muestra 26 clases con el filtro All (L-D 5, 5, 5, 5, 4, 1, 1) y 9 con Active; capturas fuera del repo. Las clases con varios entrenadores se dividen en varios `instructorIds`. Decisiones del operador del 2026-09-17: los tipos reales de Regyfit sustituyen a los programas por defecto; entrenadores como claves `regyfit-trainer-*`; clases pasadas `completed` visibles con el filtro Status. Reproducibilidad: el E2E necesita la imagen `bpt-emu-pw:local` (`bpt-emu:local` + librerias de sistema de Chromium) y un token App Check sintetico solo de emulador inyectado por la spec. **Precondiciones antes de producción:** (a) preflight de solo lectura en producción que cuente documentos de programs existentes, sesiones en la ventana, classes activas y reservas, mostrado al operador antes de APPLY (programas por defecto ya materializados o referenciados por sesiones/clases/niveles); (b) decisión del operador sobre `ageBand` de los 7 tipos infantiles/adolescentes (hoy `all` desactiva la comprobación de edad al reservar); (c) decisión sobre `completed` (no permite tomar asistencia ni editar; copy/deleteWeek lo ignoran); (d) script de rollback (borrar `createdBy == "regyfit-import"` sin reservas) y procedimiento ante fallo parcial; (e) Dockerfile de `bpt-emu-pw` versionado o referenciado. Nota (T050V2): volver a ejecutar `qa/scripts/regyfit-classes-services-map.mjs` sigue escribiendo `capacity` null para las clases "∞" de Regyfit, que tras T050V2 rechazan reservas y bloquean copy week hasta asignarles aforo. **Pendiente:** importacion en produccion hasta confirmacion en chat; inscripciones, drop-ins e historial quedan para el Plan 4b. |
| T050V2 | Planes y precios reales (11 planes, trimestre, 3x, `town-teens` retirado, `west-teens-payg`), aforo obligatorio en sesiones y copy week, límite semanal solo clases, precios públicos desde el catálogo | T046V2 | revision | Spec `docs/superpowers/specs/2026-09-17-plans-pricing-capacity-design.md`, plan `docs/superpowers/plans/2026-09-17-plans-pricing-capacity.md`, rama `feature/plans-pricing-capacity`. Evidencia (2026-09-17, sobre `0e0e738` + `410363f`): `format:check` verde; `lint` 0 avisos; `typecheck` exit 0; `test` 328 ficheros / 3058 pruebas verdes; build web verde; `test:rules` en el contenedor `bpt-emu:local` 13 ficheros / 93 pruebas verdes; `test:integration` en el contenedor: la primera pasada dio 8 pruebas rojas en `plan-adapters` y `membership-adapters` (seguían esperando 10 planes y usaban `town-teens` retirado), corregidas en `410363f` y re-ejecutadas: 29 ficheros verdes + 1 omitido / 91 pruebas verdes + 1 omitida; `test:e2e:smoke` con build de CI (clave App Check sintética): 2 verdes, 4 rojas, 2 omitidas, las mismas 4 rojas en un build de `main` `16359e8` (ajenas: `admin-shell` por App Check, `public-home` por la dirección duplicada y el CTA a `/enrol` de `4808277`); con build de emulador local: 4 verdes, 2 rojas (`public-home`, mismas causas ajenas), y las aserciones de precios de `#fees` pasan. Citado de los informes y commits de las tareas 2, 3, 5 y 9, no re-ejecutado en el HEAD final: `schedule-auth-emulator.spec.ts` 4/4 en el contenedor (con mutación `weeklyClassLimit: 3` ⇒ rojo); guardias desactivadas ⇒ rojo: copy week con sesión sin aforo (servicio, 1 roja), open mat contado en el cupo (integración `booking-transaction`, 1 roja), aforo legado `capacity-not-set` (integración, 1 roja), panel de sesión sin aforo (2 rojas). Capturas `qa/screenshots/t050-home-fees`, `t050-enrol-plans`, `t050-admin-session-capacity`, `t050-admin-plan-drift` (+ `-phone`, 1440 px y 390 px) de `qa/tests/plans-pricing-visual.spec.ts`: "£135 per term" visible, `Town Teens` ausente de la landing y de `/enrol`, error de aforo con regla roja, banda ámbar "Differs from catalogue". Oleada de correcciones de la revisión final (2026-09-17, `a4197a6`, `a600eaf`): `copyWeek` envía `failed-precondition` con `details.reason: "capacity-not-set"` y la web solo muestra el mensaje de aforo con ese motivo (pruebas `schedule-callables` y `schedule-client` rojas antes del cambio, verdes después); `typecheck`, `lint`, `format:check` verdes y `test` 328 ficheros / 3058 pruebas verdes. **Despliegue 2026-09-17 (operador Luis, en chat):** (1) aforo asignado a las 82 sesiones futuras con `capacity` null (todas a 40) con `.tmp/t050-capacity-backfill.mjs` (dry run por defecto, transaccion por sesion que re-comprueba null, project id que no empieza por `demo-` exige `PRODUCTION_CONFIRM`); probado antes en el emulador Firestore con `.tmp/t050-capacity-backfill-emu-test.mjs` 9/9 (no toca sesiones pasadas ni con aforo, re-ejecucion `updated: 0`, produccion rechazada sin confirmacion); salida real `upcomingUncapped: 82` -> `updated: 82`, verificacion posterior `upcomingUncapped: 0` (siguen sin aforo 216 sesiones pasadas). (2) `missingProgramId: []` en las 389 sesiones de produccion. (3) 48 functions desplegadas desde un worktree limpio de `origin/main` `e61031c` (las 42 del despliegue de Plan 1, sin `selfCheckIn`, mas `listPlans`, `listManagedPlans`, `getPlan`, `savePlan`, `activatePlan`, `deactivatePlan`), config temporal `firebase.deploy.tmp.json` + `.tmp/functions-env/.env` con `BPT_WAIVER_REGISTRATION=disabled`: `Deploy complete!`, 48 `Successful`, `requestBooking`/`saveSession`/`copyWeek`/`savePlan`/`getPlan`/`deactivatePlan` ACTIVE, 129 functions en el proyecto; log `.tmp/deploy-t050.log`; antes del despliegue, 178 pruebas de `memberships` + `schedule-callables` + `schedule-service` + `domain/memberships` verdes en ese worktree. Un `firebase deploy` sin `--only` (linea partida al pegar) aborto solo, sin cambios, al detectar `getMemberReportSummary` y `searchMembers` en produccion y no en `main`: no borrarlas, son de otra rama. (4) `town-teens` desactivado y (5) `west-teens-payg` creado y activo, verificados leyendo Firestore: `active: false` y `priceMinor: 750`, per-session, teens, West, sin open mats. (6) sin deriva que corregir: solo existen esos 2 documentos de plan; los otros 9 siguen "Not configured" porque `seedPlanCatalog` no lo llama ningun callable y solo se crean al guardar. (7) Los 9 planes que faltaban se crearon y activaron el 2026-09-18 con `.tmp/t050-seed-plans.mjs` (create-only desde PLAN_CATALOG, valida cada documento con `parsePlanRecord`, `active: !retiredPlanIds.includes(planId)`, project id que no empieza por `demo-` exige `PRODUCTION_CONFIRM`), probado antes en el emulador con `.tmp/t050-seed-plans-emu-test.mjs` 13/13 (un plan ya existente y editado a mano queda intacto, `town-teens` nace inactivo, re-ejecucion `created: 0`, produccion rechazada sin confirmacion); lectura posterior de Firestore: 11 planes, 10 activos y `town-teens` inactivo, con los precios del catalogo (bpt-jersey-adult 12500, town-adult 8500, town-kids-1x 9500, town-kids-2x 13500 limite 3, west-adult 6500, west-kids-1x 9500 term, west-kids-2x 11500 term, west-teens 4500, west-teens-payg 750, payg 1000). Credencial: clave de service account que el operador ya usa en otro proyecto, leida solo via `GOOGLE_APPLICATION_CREDENTIALS` desde `/root/secrets` (fuera del repositorio, `-rw-------`); la copia suelta en `/root/json` se borro con `shred`. **Restante:** (7) No revertir functions tras editar planes sin restaurar antes los planes. |

## Preguntas abiertas del operador

No queda ninguna abierta. La ultima que lo estaba, la de T025V2 -si se abre una via de
inicializacion valida para produccion y con que precondicion de vacuidad, o si se escriben los tres
documentos a mano-, se respondio el 2026-09-09 en dos tiempos: se construye la via, y **su
precondicion excluye `auditEvents` y `members`**. La segunda mitad hizo falta porque preparando el
despliegue se midio que `members` tiene 243 registros reales y que exigirla vacia dejaba a la
academia sin poder inicializar ni migrar. Es la decision D7, y su porque completo esta en T025V2.

Las siete estan resueltas, todas el 2026-09-09, y ninguna vive ya solo en el
chat. Las preguntas 5 y 6, ambas de T012V2, son las decisiones D5 y D6 de la tabla de arriba. Las
cuatro que quedaban se cerraron en la ronda de `/grill-me` de ese mismo dia, y su respuesta esta
escrita en la fila que las citaba:

- **La 1**, si la perdida de la clase por el contador emite propuesta de fee: si, la misma que un
  no-show, en la misma coleccion y con su motivo. Registrada en T015V2.
- **La 2**, si la cancelacion tardia en West genera propuesta y por que importe: si, y por los mismos
  GBP 15. Registrada en T007V2; enmienda la decision 2 del BRIEF.
- **La 3**, de donde sale la etiqueta de centro de un pago de suscripcion: del `trainingCenter` del
  miembro, tambien en los planes que cruzan sedes. Registrada en T019V2.
- **La 4**, si hay edad minima para que un menor tenga cuenta propia: 12, la misma linea que la
  academia ya traza entre Kids y Teens. Registrada en T009V2.

Construir una fila puede abrir preguntas nuevas. Cuando pase, se anotan aqui.

### Texto original de las cuatro, conservado

Estas no bloquean el arranque, pero si el cierre de la fila que las cita.

1. **T015V2 y T007V2.** La perdida automatica de la clase a los 20 minutos, żemite propuesta de fee de
   inasistencia igual que una cancelacion tardia, o solo libera la plaza? Son dos politicas
   distintas y la fila no puede cerrarse sin elegir una.
2. **T007V2.** El fee de inasistencia existente es de GBP 15 y esta limitado a Town por la decision 2
   del BRIEF. żLa cancelacion tardia en West tambien genera propuesta, y por el mismo importe?
3. **T019V2.** żLa etiqueta de centro en un pago de suscripcion sale del centro habitual del miembro
   (campo `trainingCenter`, que ya existe) o se elige en cada cobro?
4. **T009V2.** żHay edad minima para que un menor tenga cuenta propia, o cualquier menor inscrito
   puede tenerla si el representante aporta el contacto?

## Orden de ejecucion recomendado

1. **T025V2 y T012V2: HECHAS el 2026-09-09.** Eran el tapon de todo lo demas, porque mientras la
   cola de aprobacion no funcionara no entraba ningun miembro nuevo y casi todo este ledger describe
   lo que pasa despues de esa aprobacion. Hoy el directorio canonico esta inicializado en produccion
   y hay un miembro dado de alta de punta a punta desde la cuenta de `owner`. La evidencia esta en
   las dos filas.
   **T022V2 pasa a ser lo primero**, porque es lo que levanta la limitacion que D5 acepta a
   sabiendas -que la cuenta de `administrator` no sirve para el directorio canonico-, y mientras no
   este hecha la academia depende de una sola cuenta operativa. Detras de ella, dos cosas que el
   alta de hoy dejo a la vista: la migracion de los 243 miembros legacy, que sigue sin fila propia y
   sin ninguna callable desplegada, y la solicitud cuya cuenta de Auth ya no existe.
2. **T001V2, T002V2, T003V2, T004V2**: la puerta de entrada publica. Barato, visible y sin dependencias.
3. **T006V2 y T007V2**: cambian el orden del alta y la politica de cancelacion. Tocan reglas y
   callables, no solo interfaz.
4. **T018V2**: cierra la brecha entre una regla escrita y una regla aplicada.
5. **T013V2 a T017V2, y detras T024V2**: el panel de administracion, ya con altas funcionando.
   T024V2 va despues de T015V2 porque reutiliza su maquinaria programada, y cierra el
   incumplimiento de la decision 3 del BRIEF.
6. **T008V2 y T009V2**: el bloque de familias y menores, que arrastra revision de DPIA.
7. **T023V2, y despues T010V2, T011V2, T019V2 y T020V2**: cierre. T023V2 va primero porque
   T019V2 depende de ella; las tres de landing y directorio, en cuanto T021V2 aporte los datos.

## Ciclo de resolucion fila por fila

Instalado el 2026-09-09 con el mismo instalador que el resto de skills del repo: `grill-me` y
`grilling` de `mattpocock/skills` (MIT), registradas en `skills-lock.json`. `grill-me` solo lo
invoca el operador escribiendo `/grill-me`; `grilling` puede invocarse durante el trabajo.

`grilling` entrevista por **rondas**: pregunta de golpe todo lo que ya se puede decidir, con una
recomendacion por pregunta, y espera la respuesta antes de la siguiente ronda. Encaja exactamente
con el problema de este ledger, que es que hay decisiones sueltas mezcladas con trabajo mecanico.

Ciclo por fila:

1. **Interrogar.** `/grill-me` sobre la fila. Sirve para cerrar sus decisiones abiertas antes de
   escribir nada, no para repasar lo que ya esta decidido.
2. **Registrar.** La respuesta entra en la fila de `tasksv2.md`, con fecha. Una decision que solo
   vive en el chat no existe.
3. **Implementar y probar**, con el ciclo de autocritica Nivel 3 de siempre.
4. **Sincronizar.** Estado y evidencia a `tasksv2.md` y despues a `Listav2/Listav2.data.js`, en el
   mismo cambio logico. `qa/unit/listav2-ledger-sync.test.ts` falla si uno de los dos se queda
   atras.
5. **Marcar.** Cada requisito que quede resuelto se marca en `RESOLUTION_REQUIREMENTS` poniendo su
   segundo argumento a `true`. Las casillas del tablero son de solo lectura a proposito: una marca
   es una afirmacion sobre el proyecto, asi que se hace en el repositorio, donde tiene autor, fecha
   y diff, y no desde la pagina, donde viviria en un solo navegador. Solo se marca lo que se puede
   demostrar; lo que se averiguo va en `RESOLUTION_NOTES`, que no es marcable porque un hecho no se
   completa.

**Las cinco filas que habia que interrogar antes de tocarlas** -T006V2, T007V2, T009V2, T015V2 y
T019V2- **se interrogaron el 2026-09-09**, en tres rondas, y las veinticuatro decisiones que salieron
estan escritas en sus filas con esa fecha. Ya se pueden construir. La ronda produjo ademas dos filas
nuevas, T023V2 y T024V2, dos enmiendas al BRIEF -sus decisiones 2 y 6- y una correccion a lo que
T015V2 afirmaba sobre open mat, que era falso. Las demas filas ya tenian la causa localizada y se
pueden empezar de frente.

T021V2 es la excepcion: no se resuelve interrogando, porque lo que falta son datos que solo tiene el
operador. Ninguna cantidad de preguntas produce el domicilio de West.

## Repartir el trabajo entre dos personas

<!-- REPARTO:INICIO -->

<!-- Generado por `node Listav2/parallel-report.mjs`. No lo edites a mano: la superficie de
     cada fila se declara en TASK_SURFACES, dentro de Listav2/Listav2.data.js. -->

Dos filas se pueden repartir entre dos personas cuando se cumplen **las tres** a la vez:
ninguna encadena a la otra, las dos estan listas para empezar -sin dependencia abierta- y no
escriben en el mismo fichero. Esto se calcula, no se afirma: si una fila cambia de superficie,
el reparto cambia solo.

**Superficie sin declarar no quiere decir compatible con todo**, quiere decir que no se puede
afirmar nada. Esas filas no se reparten hasta que alguien declare que ficheros van a escribir.

La ultima columna responde a la otra pregunta, la que hay que hacerse antes de coger una fila:
**quien mas escribe estos ficheros, y en que estado esta**. `en curso` es una fila que alguien
ya empezo -no la cojas contra ella-; `sin empezar` es un choque futuro que se evita eligiendo
el orden; `ya cerrada` es una fila desplegada, aprobada o cancelada, que no compite con nadie
y solo dice quien toco ese fichero el ultimo.

Hoy hay **9 filas listas** de 43 abiertas, y ninguna con la superficie sin declarar. **16 filas estan en curso**: T026V2, T027V2, T028V2, T029V2, T030V2, T031V2, T032V2, T033V2, T034V2, T035V2, T036V2, T037V2, T038V2, T039V2, T046V2, T050V2.

| Fila | Estado | Toca | Puede ir a la vez que | Interfiere con |
| ---- | ------ | ---- | --------------------- | -------------- |
| T002V2 | lista | `apps/web/src/app/enrol/page.tsx` | T005V2, T007V2, T008V2, T013V2, T018V2, T020V2, T023V2, T026V2, T027V2, T029V2, T030V2, T031V2, T032V2, T034V2, T036V2, T042V2, T046V2 | T050V2 (en curso) en `apps/web/src/app/enrol/page.tsx`<br>T003V2 (sin empezar) en `apps/web/src/app/enrol/page.tsx`<br>T001V2 (ya cerrada) en `apps/web/src/app/enrol/page.tsx` |
| T003V2 | pendiente | `apps/web/src/app/enrol/page.tsx`<br>`apps/web/src/app/enrol/enrol.css` | espera a T002V2 | T050V2 (en curso) en `apps/web/src/app/enrol/page.tsx`, `apps/web/src/app/enrol/enrol.css`<br>T002V2 (sin empezar) en `apps/web/src/app/enrol/page.tsx`<br>T001V2 (ya cerrada) en `apps/web/src/app/enrol/page.tsx` |
| T004V2 | pendiente | `apps/web/src/app/page.tsx`<br>`apps/web/src/lib/client-auth.tsx` | espera a T002V2 | T050V2 (en curso) en `apps/web/src/app/page.tsx`<br>T011V2 (sin empezar) en `apps/web/src/app/page.tsx` |
| T005V2 | lista | `packages/domain/src/consents/enrolment-waiver-terms.ts` | T002V2, T007V2, T008V2, T013V2, T018V2, T020V2, T023V2, T026V2, T027V2, T029V2, T030V2, T031V2, T032V2, T034V2, T036V2, T042V2, T046V2 | - |
| T006V2 | pendiente | `apps/functions/src/schedule/schedule-callables.ts`<br>`packages/domain/src/members/enrolment-request-contracts.ts` | espera a T002V2, T004V2 | T032V2 (en curso) en `apps/functions/src/schedule/schedule-callables.ts`<br>T033V2 (en curso) en `apps/functions/src/schedule/schedule-callables.ts`<br>T046V2 (en curso) en `apps/functions/src/schedule/schedule-callables.ts`<br>T050V2 (en curso) en `apps/functions/src/schedule/schedule-callables.ts`<br>T040V2 (ya cerrada) en `apps/functions/src/schedule/schedule-callables.ts` |
| T007V2 | lista | `packages/domain/src/schedule/schedule-contracts.ts`<br>`packages/domain/src/penalties/no-show-penalty-contracts.ts` | T002V2, T005V2, T008V2, T013V2, T020V2, T023V2, T026V2, T027V2, T029V2, T030V2, T031V2, T034V2, T036V2, T042V2 | T032V2 (en curso) en `packages/domain/src/schedule/schedule-contracts.ts`<br>T046V2 (en curso) en `packages/domain/src/schedule/schedule-contracts.ts`<br>T050V2 (en curso) en `packages/domain/src/schedule/schedule-contracts.ts`<br>T015V2 (sin empezar) en `packages/domain/src/schedule/schedule-contracts.ts`<br>T018V2 (sin empezar) en `packages/domain/src/schedule/schedule-contracts.ts`<br>T040V2 (ya cerrada) en `packages/domain/src/schedule/schedule-contracts.ts` |
| T008V2 | lista | `apps/web/src/app/account/family/page.tsx` | T002V2, T005V2, T007V2, T013V2, T018V2, T020V2, T023V2, T026V2, T027V2, T029V2, T030V2, T031V2, T032V2, T034V2, T036V2, T042V2, T046V2 | T038V2 (en curso) en `apps/web/src/app/account/family/page.tsx`<br>T050V2 (en curso) en `apps/web/src/app/account/family/page.tsx` |
| T009V2 | pendiente | `apps/functions/src/families/family-service.ts`<br>`packages/domain/src/profiles/profile-contracts.ts`<br>`apps/web/src/lib/auth-client.ts`<br>`apps/functions/src/delivery/delivery-service.ts`<br>`docs/operations/t011-dpia-draft.md` | espera a T008V2 | T044V2 (sin empezar) en `apps/functions/src/families/family-service.ts`<br>T041V2 (ya cerrada) en `packages/domain/src/profiles/profile-contracts.ts` |
| T042V2 | lista | `apps/functions/src/streak`<br>`apps/web/src/lib/streak-client.ts`<br>`apps/web/src/app/account/streak`<br>`apps/functions/src/deploy-runtime.ts` | T002V2, T005V2, T007V2, T008V2, T013V2, T018V2, T020V2, T023V2, T026V2, T027V2, T029V2, T030V2, T031V2, T032V2, T034V2, T036V2 | T038V2 (en curso) en `apps/web/src/app/account/streak`<br>T046V2 (en curso) en `apps/functions/src/deploy-runtime.ts`<br>T050V2 (en curso) en `apps/web/src/app/account/streak`<br>T043V2 (sin empezar) en `apps/functions/src/deploy-runtime.ts`<br>T044V2 (sin empezar) en `apps/functions/src/deploy-runtime.ts`<br>T040V2 (ya cerrada) en `apps/functions/src/deploy-runtime.ts`<br>T041V2 (ya cerrada) en `apps/functions/src/deploy-runtime.ts` |
| T043V2 | pendiente | `apps/functions/src/competitors`<br>`apps/web/src/lib/competitors-client.ts`<br>`apps/web/src/app/account/competitors`<br>`apps/functions/src/deploy-runtime.ts` | espera a T008V2, T009V2, T044V2 | T038V2 (en curso) en `apps/web/src/app/account/competitors`<br>T046V2 (en curso) en `apps/functions/src/deploy-runtime.ts`<br>T050V2 (en curso) en `apps/web/src/app/account/competitors`<br>T042V2 (sin empezar) en `apps/functions/src/deploy-runtime.ts`<br>T044V2 (sin empezar) en `apps/functions/src/deploy-runtime.ts`<br>T040V2 (ya cerrada) en `apps/functions/src/deploy-runtime.ts`<br>T041V2 (ya cerrada) en `apps/functions/src/deploy-runtime.ts` |
| T044V2 | pendiente | `apps/functions/src/account-settings`<br>`apps/functions/src/profiles/profile-service.ts`<br>`apps/functions/src/families/family-service.ts`<br>`apps/web/src/lib/account-settings-client.ts`<br>`apps/web/src/app/account/settings`<br>`apps/functions/src/deploy-runtime.ts` | espera a T008V2, T009V2 | T038V2 (en curso) en `apps/web/src/app/account/settings`<br>T046V2 (en curso) en `apps/functions/src/deploy-runtime.ts`<br>T050V2 (en curso) en `apps/web/src/app/account/settings`<br>T009V2 (sin empezar) en `apps/functions/src/families/family-service.ts`<br>T020V2 (sin empezar) en `apps/functions/src/profiles/profile-service.ts`<br>T042V2 (sin empezar) en `apps/functions/src/deploy-runtime.ts`<br>T043V2 (sin empezar) en `apps/functions/src/deploy-runtime.ts`<br>T040V2 (ya cerrada) en `apps/functions/src/deploy-runtime.ts`<br>T041V2 (ya cerrada) en `apps/functions/src/deploy-runtime.ts` |
| T010V2 | pendiente | `apps/web/src/content/academy.ts` | espera a T021V2 | T011V2 (sin empezar) en `apps/web/src/content/academy.ts` |
| T011V2 | pendiente | `apps/web/src/content/academy.ts`<br>`apps/web/src/app/page.tsx` | espera a T021V2 | T050V2 (en curso) en `apps/web/src/app/page.tsx`<br>T004V2 (sin empezar) en `apps/web/src/app/page.tsx`<br>T010V2 (sin empezar) en `apps/web/src/content/academy.ts` |
| T013V2 | lista | `apps/web/src/app/admin/overview-page.tsx` | T002V2, T005V2, T007V2, T008V2, T018V2, T020V2, T023V2, T026V2, T029V2, T030V2, T031V2, T032V2, T034V2, T036V2, T042V2, T046V2 | T027V2 (en curso) en `apps/web/src/app/admin/overview-page.tsx`<br>T014V2 (sin empezar) en `apps/web/src/app/admin/overview-page.tsx`<br>T016V2 (sin empezar) en `apps/web/src/app/admin/overview-page.tsx`<br>T017V2 (sin empezar) en `apps/web/src/app/admin/overview-page.tsx` |
| T014V2 | pendiente | `apps/web/src/app/admin/overview-page.tsx` | espera a T013V2 | T027V2 (en curso) en `apps/web/src/app/admin/overview-page.tsx`<br>T013V2 (sin empezar) en `apps/web/src/app/admin/overview-page.tsx`<br>T016V2 (sin empezar) en `apps/web/src/app/admin/overview-page.tsx`<br>T017V2 (sin empezar) en `apps/web/src/app/admin/overview-page.tsx` |
| T015V2 | pendiente | `packages/domain/src/schedule/schedule-contracts.ts`<br>`packages/domain/src/memberships/plan-contracts.ts`<br>`apps/functions/src/schedule/booking-transaction-service.ts` | espera a T013V2, T014V2 | T032V2 (en curso) en `packages/domain/src/schedule/schedule-contracts.ts`, `apps/functions/src/schedule/booking-transaction-service.ts`<br>T033V2 (en curso) en `apps/functions/src/schedule/booking-transaction-service.ts`<br>T046V2 (en curso) en `packages/domain/src/schedule/schedule-contracts.ts`, `apps/functions/src/schedule/booking-transaction-service.ts`<br>T050V2 (en curso) en `packages/domain/src/schedule/schedule-contracts.ts`, `packages/domain/src/memberships/plan-contracts.ts`, `apps/functions/src/schedule/booking-transaction-service.ts`<br>T007V2 (sin empezar) en `packages/domain/src/schedule/schedule-contracts.ts`<br>T018V2 (sin empezar) en `packages/domain/src/schedule/schedule-contracts.ts`<br>T023V2 (sin empezar) en `apps/functions/src/schedule/booking-transaction-service.ts`<br>T047V2 (sin empezar) en `packages/domain/src/memberships/plan-contracts.ts`<br>T040V2 (ya cerrada) en `packages/domain/src/schedule/schedule-contracts.ts` |
| T016V2 | pendiente | `apps/web/src/app/admin/overview-page.tsx` | espera a T013V2 | T027V2 (en curso) en `apps/web/src/app/admin/overview-page.tsx`<br>T013V2 (sin empezar) en `apps/web/src/app/admin/overview-page.tsx`<br>T014V2 (sin empezar) en `apps/web/src/app/admin/overview-page.tsx`<br>T017V2 (sin empezar) en `apps/web/src/app/admin/overview-page.tsx` |
| T017V2 | pendiente | `apps/web/src/app/admin/overview-page.tsx` | espera a T013V2 | T027V2 (en curso) en `apps/web/src/app/admin/overview-page.tsx`<br>T013V2 (sin empezar) en `apps/web/src/app/admin/overview-page.tsx`<br>T014V2 (sin empezar) en `apps/web/src/app/admin/overview-page.tsx`<br>T016V2 (sin empezar) en `apps/web/src/app/admin/overview-page.tsx` |
| T024V2 | pendiente | `apps/web/src/lib/schedule-client.ts`<br>`apps/functions/src/schedule/quorum-sweep-runner.ts` | espera a T013V2, T014V2, T015V2 | T032V2 (en curso) en `apps/web/src/lib/schedule-client.ts`, `apps/functions/src/schedule/quorum-sweep-runner.ts`<br>T033V2 (en curso) en `apps/functions/src/schedule/quorum-sweep-runner.ts`<br>T046V2 (en curso) en `apps/web/src/lib/schedule-client.ts`, `apps/functions/src/schedule/quorum-sweep-runner.ts`<br>T050V2 (en curso) en `apps/functions/src/schedule/quorum-sweep-runner.ts`<br>T040V2 (ya cerrada) en `apps/web/src/lib/schedule-client.ts` |
| T026V2 | **en curso** | `apps/web/src/app/admin/admin-shell.tsx`<br>`apps/web/src/app/admin/admin.css` | T002V2, T005V2, T007V2, T008V2, T013V2, T018V2, T020V2, T023V2, T027V2, T029V2, T030V2, T032V2, T034V2, T036V2, T042V2 | T031V2 (en curso) en `apps/web/src/app/admin/admin-shell.tsx`<br>T046V2 (en curso) en `apps/web/src/app/admin/admin-shell.tsx` |
| T027V2 | **en curso** | `apps/web/src/app/admin/overview-page.tsx` | T002V2, T005V2, T007V2, T008V2, T018V2, T020V2, T023V2, T026V2, T029V2, T030V2, T031V2, T032V2, T034V2, T036V2, T042V2, T046V2 | T013V2 (sin empezar) en `apps/web/src/app/admin/overview-page.tsx`<br>T014V2 (sin empezar) en `apps/web/src/app/admin/overview-page.tsx`<br>T016V2 (sin empezar) en `apps/web/src/app/admin/overview-page.tsx`<br>T017V2 (sin empezar) en `apps/web/src/app/admin/overview-page.tsx` |
| T028V2 | **en curso** | `apps/web/src/app/admin/attendance` | espera a T027V2 | - |
| T029V2 | **en curso** | `apps/web/src/app/admin/members/requests/page.tsx` | T002V2, T005V2, T007V2, T008V2, T013V2, T018V2, T020V2, T023V2, T026V2, T027V2, T030V2, T031V2, T032V2, T034V2, T036V2, T042V2, T046V2 | T012V2 (ya cerrada) en `apps/web/src/app/admin/members/requests/page.tsx` |
| T030V2 | **en curso** | `apps/functions/src/health`<br>`apps/web/src/app/admin/members/medical` | T002V2, T005V2, T007V2, T008V2, T013V2, T018V2, T020V2, T023V2, T026V2, T027V2, T029V2, T031V2, T032V2, T034V2, T036V2, T042V2, T046V2 | - |
| T031V2 | **en curso** | `apps/web/src/app/admin/admin-routes.ts`<br>`apps/web/src/app/admin/admin-shell.tsx` | T002V2, T005V2, T007V2, T008V2, T013V2, T018V2, T020V2, T023V2, T027V2, T029V2, T030V2, T032V2, T034V2, T036V2, T042V2 | T026V2 (en curso) en `apps/web/src/app/admin/admin-shell.tsx`<br>T037V2 (en curso) en `apps/web/src/app/admin/admin-routes.ts`<br>T046V2 (en curso) en `apps/web/src/app/admin/admin-shell.tsx` |
| T032V2 | **en curso** | `packages/domain/src/schedule/schedule-contracts.ts`<br>`apps/functions/src/schedule`<br>`apps/web/src/app/admin/classes`<br>`apps/web/src/lib/schedule-client.ts` | T002V2, T005V2, T008V2, T013V2, T020V2, T026V2, T027V2, T029V2, T030V2, T031V2, T034V2, T036V2, T042V2 | T033V2 (en curso) en `apps/functions/src/schedule`, `apps/web/src/app/admin/classes`<br>T046V2 (en curso) en `packages/domain/src/schedule/schedule-contracts.ts`, `apps/functions/src/schedule`, `apps/web/src/lib/schedule-client.ts`<br>T050V2 (en curso) en `packages/domain/src/schedule/schedule-contracts.ts`, `apps/functions/src/schedule`<br>T006V2 (sin empezar) en `apps/functions/src/schedule/schedule-callables.ts`<br>T007V2 (sin empezar) en `packages/domain/src/schedule/schedule-contracts.ts`<br>T015V2 (sin empezar) en `packages/domain/src/schedule/schedule-contracts.ts`, `apps/functions/src/schedule/booking-transaction-service.ts`<br>T018V2 (sin empezar) en `packages/domain/src/schedule/schedule-contracts.ts`<br>T023V2 (sin empezar) en `apps/functions/src/schedule/booking-transaction-service.ts`<br>T024V2 (sin empezar) en `apps/functions/src/schedule/quorum-sweep-runner.ts`, `apps/web/src/lib/schedule-client.ts`<br>T040V2 (ya cerrada) en `packages/domain/src/schedule/schedule-contracts.ts`, `apps/functions/src/schedule/attendance-transaction-service.ts`, `apps/functions/src/schedule/canonical-client-student-scope.ts`, `apps/functions/src/schedule/canonical-client-student-scope.test.ts`, `apps/functions/src/schedule/schedule-callables.ts`, `apps/functions/src/schedule/schedule-callables.test.ts`, `apps/functions/src/schedule/schedule-security-boundary.test.ts`, `apps/functions/src/schedule/schedule-service.ts`, `apps/web/src/lib/schedule-client.ts` |
| T033V2 | **en curso** | `apps/functions/src/schedule`<br>`apps/web/src/app/admin/classes` | espera a T032V2 | T032V2 (en curso) en `apps/functions/src/schedule`, `apps/web/src/app/admin/classes`<br>T046V2 (en curso) en `apps/functions/src/schedule`<br>T050V2 (en curso) en `apps/functions/src/schedule`<br>T006V2 (sin empezar) en `apps/functions/src/schedule/schedule-callables.ts`<br>T015V2 (sin empezar) en `apps/functions/src/schedule/booking-transaction-service.ts`<br>T023V2 (sin empezar) en `apps/functions/src/schedule/booking-transaction-service.ts`<br>T024V2 (sin empezar) en `apps/functions/src/schedule/quorum-sweep-runner.ts`<br>T040V2 (ya cerrada) en `apps/functions/src/schedule/attendance-transaction-service.ts`, `apps/functions/src/schedule/canonical-client-student-scope.ts`, `apps/functions/src/schedule/canonical-client-student-scope.test.ts`, `apps/functions/src/schedule/schedule-callables.ts`, `apps/functions/src/schedule/schedule-callables.test.ts`, `apps/functions/src/schedule/schedule-security-boundary.test.ts`, `apps/functions/src/schedule/schedule-service.ts` |
| T034V2 | **en curso** | `apps/web/src/app/admin/levels`<br>`DESIGN.md` | T002V2, T005V2, T007V2, T008V2, T013V2, T018V2, T020V2, T023V2, T026V2, T027V2, T029V2, T030V2, T031V2, T032V2, T036V2, T042V2, T046V2 | T040V2 (ya cerrada) en `DESIGN.md` |
| T035V2 | **en curso** | `apps/web/src/app/admin/billing` | espera a T036V2 | T036V2 (en curso) en `apps/web/src/app/admin/billing` |
| T036V2 | **en curso** | `apps/functions/src/finance`<br>`apps/web/src/app/admin/billing` | T002V2, T005V2, T007V2, T008V2, T013V2, T018V2, T020V2, T023V2, T026V2, T027V2, T029V2, T030V2, T031V2, T032V2, T034V2, T042V2, T046V2 | T035V2 (en curso) en `apps/web/src/app/admin/billing`<br>T019V2 (sin empezar) en `apps/functions/src/finance/finance-service.ts` |
| T037V2 | **en curso** | `apps/web/src/app/admin/admin-routes.ts`<br>`docs/adr` | espera a T031V2 | T031V2 (en curso) en `apps/web/src/app/admin/admin-routes.ts` |
| T038V2 | **en curso** | `apps/web/src/lib/calendar`<br>`apps/web/src/app/account` | espera a T032V2 | T050V2 (en curso) en `apps/web/src/lib/calendar`, `apps/web/src/app/account`<br>T008V2 (sin empezar) en `apps/web/src/app/account/family/page.tsx`<br>T042V2 (sin empezar) en `apps/web/src/app/account/streak`<br>T043V2 (sin empezar) en `apps/web/src/app/account/competitors`<br>T044V2 (sin empezar) en `apps/web/src/app/account/settings`<br>T040V2 (ya cerrada) en `apps/web/src/lib/calendar/calendar-repository.ts`, `apps/web/src/lib/calendar/fixture-calendar-repository.ts`, `apps/web/src/lib/calendar/fixture-calendar-repository.test.ts`, `apps/web/src/lib/calendar/firebase-calendar-repository.ts`, `apps/web/src/lib/calendar/firebase-calendar-repository.test.ts`, `apps/web/src/lib/calendar/index.ts`, `apps/web/src/lib/calendar/index.test.ts`, `apps/web/src/lib/calendar/self-check-in-messages.ts`, `apps/web/src/lib/calendar/self-check-in-messages.test.ts`, `apps/web/src/app/account/account.css`, `apps/web/src/app/account/calendar/ready-for-jiu-jitsu.tsx`, `apps/web/src/app/account/calendar/ready-for-jiu-jitsu.test.tsx`, `apps/web/src/app/account/calendar/member-calendar.tsx`, `apps/web/src/app/account/calendar/member-calendar.test.tsx`<br>T041V2 (ya cerrada) en `apps/web/src/app/account/page.tsx`, `apps/web/src/app/account/calendar/member-calendar.tsx`, `apps/web/src/app/account/calendar/calendar-header.tsx` |
| T039V2 | **en curso** | `qa/tests` | espera a T027V2 | T046V2 (en curso) en `qa/tests/admin-classes-services.spec.ts`<br>T050V2 (en curso) en `qa/tests/schedule-auth-emulator.spec.ts`<br>T040V2 (ya cerrada) en `qa/tests/account-self-check-in.spec.ts`, `qa/tests/schedule-auth-emulator.spec.ts` |
| T018V2 | lista | `packages/domain/src/schedule/schedule-contracts.ts`<br>`apps/functions/src/memberships/membership-callables.ts`<br>`packages/domain/src/finance/financial-dashboard.ts` | T002V2, T005V2, T008V2, T013V2, T020V2, T023V2, T026V2, T027V2, T029V2, T030V2, T031V2, T034V2, T036V2, T042V2 | T032V2 (en curso) en `packages/domain/src/schedule/schedule-contracts.ts`<br>T046V2 (en curso) en `packages/domain/src/schedule/schedule-contracts.ts`<br>T050V2 (en curso) en `packages/domain/src/schedule/schedule-contracts.ts`, `apps/functions/src/memberships/membership-callables.ts`<br>T007V2 (sin empezar) en `packages/domain/src/schedule/schedule-contracts.ts`<br>T015V2 (sin empezar) en `packages/domain/src/schedule/schedule-contracts.ts`<br>T047V2 (sin empezar) en `apps/functions/src/memberships/membership-callables.ts`<br>T040V2 (ya cerrada) en `packages/domain/src/schedule/schedule-contracts.ts`, `apps/functions/src/memberships/membership-callables.ts` |
| T019V2 | pendiente | `apps/functions/src/finance/finance-service.ts`<br>`packages/domain/src/shop` | espera a T023V2 | T036V2 (en curso) en `apps/functions/src/finance/finance-service.ts` |
| T020V2 | lista | `apps/web/src/app/admin/members/page.tsx`<br>`apps/functions/src/profiles/profile-service.ts` | T002V2, T005V2, T007V2, T008V2, T013V2, T018V2, T023V2, T026V2, T027V2, T029V2, T030V2, T031V2, T032V2, T034V2, T036V2, T042V2, T046V2 | T044V2 (sin empezar) en `apps/functions/src/profiles/profile-service.ts` |
| T023V2 | lista | `packages/domain/src/families/family-contracts.ts`<br>`apps/functions/src/schedule/booking-transaction-service.ts` | T002V2, T005V2, T007V2, T008V2, T013V2, T018V2, T020V2, T026V2, T027V2, T029V2, T030V2, T031V2, T034V2, T036V2, T042V2 | T032V2 (en curso) en `apps/functions/src/schedule/booking-transaction-service.ts`<br>T033V2 (en curso) en `apps/functions/src/schedule/booking-transaction-service.ts`<br>T046V2 (en curso) en `apps/functions/src/schedule/booking-transaction-service.ts`<br>T050V2 (en curso) en `apps/functions/src/schedule/booking-transaction-service.ts`<br>T015V2 (sin empezar) en `apps/functions/src/schedule/booking-transaction-service.ts` |
| T021V2 | bloqueada | no toca codigo | no esta lista | - |
| T046V2 | **en curso** | `packages/domain/src/schedule`<br>`apps/functions/src/schedule`<br>`apps/functions/src/index.ts`<br>`apps/functions/src/deploy-runtime.ts`<br>`apps/web/src/lib/schedule-client.ts`<br>`apps/web/src/app/admin/classes-services`<br>`apps/web/src/app/admin/admin-shell.tsx`<br>`qa/tests/admin-classes-services.spec.ts` | T002V2, T005V2, T008V2, T013V2, T020V2, T027V2, T029V2, T030V2, T034V2, T036V2 | T026V2 (en curso) en `apps/web/src/app/admin/admin-shell.tsx`<br>T031V2 (en curso) en `apps/web/src/app/admin/admin-shell.tsx`<br>T032V2 (en curso) en `packages/domain/src/schedule/schedule-contracts.ts`, `apps/functions/src/schedule`, `apps/web/src/lib/schedule-client.ts`<br>T033V2 (en curso) en `apps/functions/src/schedule`<br>T039V2 (en curso) en `qa/tests/admin-classes-services.spec.ts`<br>T050V2 (en curso) en `packages/domain/src/schedule`, `apps/functions/src/schedule`, `apps/web/src/app/admin/classes-services`<br>T006V2 (sin empezar) en `apps/functions/src/schedule/schedule-callables.ts`<br>T007V2 (sin empezar) en `packages/domain/src/schedule/schedule-contracts.ts`<br>T015V2 (sin empezar) en `packages/domain/src/schedule/schedule-contracts.ts`, `apps/functions/src/schedule/booking-transaction-service.ts`<br>T018V2 (sin empezar) en `packages/domain/src/schedule/schedule-contracts.ts`<br>T023V2 (sin empezar) en `apps/functions/src/schedule/booking-transaction-service.ts`<br>T024V2 (sin empezar) en `apps/functions/src/schedule/quorum-sweep-runner.ts`, `apps/web/src/lib/schedule-client.ts`<br>T042V2 (sin empezar) en `apps/functions/src/deploy-runtime.ts`<br>T043V2 (sin empezar) en `apps/functions/src/deploy-runtime.ts`<br>T044V2 (sin empezar) en `apps/functions/src/deploy-runtime.ts`<br>T047V2 (sin empezar) en `apps/web/src/app/admin/classes-services`<br>T048V2 (sin empezar) en `apps/web/src/app/admin/classes-services`<br>T022V2 (ya cerrada) en `apps/functions/src/index.ts`<br>T040V2 (ya cerrada) en `packages/domain/src/schedule/schedule-contracts.ts`, `packages/domain/src/schedule/self-check-in-contracts.ts`, `packages/domain/src/schedule/self-check-in-contracts.test.ts`, `apps/functions/src/schedule/attendance-transaction-service.ts`, `apps/functions/src/schedule/canonical-client-student-scope.ts`, `apps/functions/src/schedule/canonical-client-student-scope.test.ts`, `apps/functions/src/schedule/schedule-callables.ts`, `apps/functions/src/schedule/schedule-callables.test.ts`, `apps/functions/src/schedule/schedule-security-boundary.test.ts`, `apps/functions/src/schedule/schedule-service.ts`, `apps/functions/src/index.ts`, `apps/functions/src/deploy-runtime.ts`, `apps/web/src/lib/schedule-client.ts`<br>T041V2 (ya cerrada) en `apps/functions/src/index.ts`, `apps/functions/src/deploy-runtime.ts` |
| T047V2 | pendiente | `packages/domain/src/memberships`<br>`apps/functions/src/memberships`<br>`apps/web/src/app/admin/classes-services` | espera a T046V2 | T046V2 (en curso) en `apps/web/src/app/admin/classes-services`<br>T050V2 (en curso) en `packages/domain/src/memberships`, `apps/functions/src/memberships`, `apps/web/src/app/admin/classes-services`<br>T015V2 (sin empezar) en `packages/domain/src/memberships/plan-contracts.ts`<br>T018V2 (sin empezar) en `apps/functions/src/memberships/membership-callables.ts`<br>T048V2 (sin empezar) en `apps/web/src/app/admin/classes-services`<br>T040V2 (ya cerrada) en `apps/functions/src/memberships/membership-callables.ts`, `apps/functions/src/memberships/membership-callables.test.ts` |
| T048V2 | pendiente | `apps/functions/src/classes-services-reports`<br>`apps/web/src/app/admin/classes-services` | espera a T046V2, T047V2 | T046V2 (en curso) en `apps/web/src/app/admin/classes-services`<br>T050V2 (en curso) en `apps/web/src/app/admin/classes-services`<br>T047V2 (sin empezar) en `apps/web/src/app/admin/classes-services` |
| T049V2 | pendiente | `apps/functions/src/migrations/regyfit`<br>`docs/data/migrations/regyfit` | espera a T046V2, T047V2, T048V2 | - |
| T050V2 | **en curso** | `packages/domain/src/memberships`<br>`packages/domain/src/schedule`<br>`apps/functions/src/memberships`<br>`apps/functions/src/schedule`<br>`apps/web/src/app/admin/classes-services`<br>`apps/web/src/app/admin/memberships`<br>`apps/web/src/app/account`<br>`apps/web/src/app/enrol`<br>`apps/web/src/app/page.tsx`<br>`apps/web/src/lib/calendar`<br>`qa/tests/schedule-auth-emulator.spec.ts` | espera a T046V2 | T032V2 (en curso) en `packages/domain/src/schedule/schedule-contracts.ts`, `apps/functions/src/schedule`<br>T033V2 (en curso) en `apps/functions/src/schedule`<br>T038V2 (en curso) en `apps/web/src/app/account`, `apps/web/src/lib/calendar`<br>T039V2 (en curso) en `qa/tests/schedule-auth-emulator.spec.ts`<br>T046V2 (en curso) en `packages/domain/src/schedule`, `apps/functions/src/schedule`, `apps/web/src/app/admin/classes-services`<br>T002V2 (sin empezar) en `apps/web/src/app/enrol/page.tsx`<br>T003V2 (sin empezar) en `apps/web/src/app/enrol/page.tsx`, `apps/web/src/app/enrol/enrol.css`<br>T004V2 (sin empezar) en `apps/web/src/app/page.tsx`<br>T006V2 (sin empezar) en `apps/functions/src/schedule/schedule-callables.ts`<br>T007V2 (sin empezar) en `packages/domain/src/schedule/schedule-contracts.ts`<br>T008V2 (sin empezar) en `apps/web/src/app/account/family/page.tsx`<br>T011V2 (sin empezar) en `apps/web/src/app/page.tsx`<br>T015V2 (sin empezar) en `packages/domain/src/memberships/plan-contracts.ts`, `packages/domain/src/schedule/schedule-contracts.ts`, `apps/functions/src/schedule/booking-transaction-service.ts`<br>T018V2 (sin empezar) en `packages/domain/src/schedule/schedule-contracts.ts`, `apps/functions/src/memberships/membership-callables.ts`<br>T023V2 (sin empezar) en `apps/functions/src/schedule/booking-transaction-service.ts`<br>T024V2 (sin empezar) en `apps/functions/src/schedule/quorum-sweep-runner.ts`<br>T042V2 (sin empezar) en `apps/web/src/app/account/streak`<br>T043V2 (sin empezar) en `apps/web/src/app/account/competitors`<br>T044V2 (sin empezar) en `apps/web/src/app/account/settings`<br>T047V2 (sin empezar) en `packages/domain/src/memberships`, `apps/functions/src/memberships`, `apps/web/src/app/admin/classes-services`<br>T048V2 (sin empezar) en `apps/web/src/app/admin/classes-services`<br>T001V2 (ya cerrada) en `apps/web/src/app/enrol/page.tsx`<br>T040V2 (ya cerrada) en `packages/domain/src/schedule/schedule-contracts.ts`, `packages/domain/src/schedule/self-check-in-contracts.ts`, `packages/domain/src/schedule/self-check-in-contracts.test.ts`, `apps/functions/src/memberships/membership-callables.ts`, `apps/functions/src/memberships/membership-callables.test.ts`, `apps/functions/src/schedule/attendance-transaction-service.ts`, `apps/functions/src/schedule/canonical-client-student-scope.ts`, `apps/functions/src/schedule/canonical-client-student-scope.test.ts`, `apps/functions/src/schedule/schedule-callables.ts`, `apps/functions/src/schedule/schedule-callables.test.ts`, `apps/functions/src/schedule/schedule-security-boundary.test.ts`, `apps/functions/src/schedule/schedule-service.ts`, `apps/web/src/app/account/account.css`, `apps/web/src/app/account/calendar/ready-for-jiu-jitsu.tsx`, `apps/web/src/app/account/calendar/ready-for-jiu-jitsu.test.tsx`, `apps/web/src/app/account/calendar/member-calendar.tsx`, `apps/web/src/app/account/calendar/member-calendar.test.tsx`, `apps/web/src/lib/calendar/calendar-repository.ts`, `apps/web/src/lib/calendar/fixture-calendar-repository.ts`, `apps/web/src/lib/calendar/fixture-calendar-repository.test.ts`, `apps/web/src/lib/calendar/firebase-calendar-repository.ts`, `apps/web/src/lib/calendar/firebase-calendar-repository.test.ts`, `apps/web/src/lib/calendar/index.ts`, `apps/web/src/lib/calendar/index.test.ts`, `apps/web/src/lib/calendar/self-check-in-messages.ts`, `apps/web/src/lib/calendar/self-check-in-messages.test.ts`, `qa/tests/schedule-auth-emulator.spec.ts`<br>T041V2 (ya cerrada) en `apps/web/src/app/account/page.tsx`, `apps/web/src/app/account/calendar/member-calendar.tsx`, `apps/web/src/app/account/calendar/calendar-header.tsx` |

<!-- REPARTO:FIN -->

**Como se usa.** Cada uno coge una fila de las que la tabla marca como listas, comprueba que la otra
aparezca en su columna "Puede ir a la vez que", y adelante. Al terminar, cada uno sube lo suyo. Si
una fila no aparece ahi pero si en "Interfiere con", la tabla dice **en que fichero**, que es lo que
hace falta para decidir si una de las dos se puede reordenar o partir en vez de descartar el
paralelo a ciegas.

**Lo primero que hay que mirar en "Interfiere con" es el estado entre parentesis**, no el ID. `en
curso` es una fila que alguien ya empezo: coger contra ella es escribir en un fichero que otro esta
tocando ahora mismo. `sin empezar` es un choque futuro -nadie escribe todavia, y basta con acordar
el orden-. `ya cerrada` no es un riesgo: la fila esta desplegada, aprobada o cancelada y su cambio
ya cayo; la linea solo dice quien toco ese fichero el ultimo. Es la diferencia que hace util la
columna: la pregunta de verdad no es "esta desplegada?", sino "hay alguien escribiendo ahora en lo
que voy a tocar?".

**Esta columna solo dice la verdad si el estado se actualiza al empezar.** Una fila que alguien esta
escribiendo pero sigue puesta como `pendiente` sale como `sin empezar`, y entonces la tabla afirma
que no hay nadie donde si lo hay. Poner la fila en `en-progreso` en `tasksv2.md` -y sincronizar el
tablero- es lo que enciende el aviso para el otro. Con dos personas repartiendose el trabajo, ese
paso deja de ser burocracia y pasa a ser la senal.

**Donde se declara la superficie.** En `TASK_SURFACES`, dentro de `Listav2/Listav2.data.js`. Es la
unica excepcion a que el ledger mande: el estado y la evidencia de una fila se escriben aqui
primero, pero la superficie vive alli porque alli ya vivian las referencias por tarea, y tenerla en
dos sitios garantizaria que las dos copias se separen. La tabla de arriba se regenera con
`node Listav2/parallel-report.mjs`, y una prueba falla si se queda vieja.

**Cuidado con una cosa.** La superficie dice donde va a **caer el cambio**, no donde se encontro el
problema. Una fila que arregla un bug en un fichero pero toca tres al arreglarlo tiene tres en su
superficie, y declarar solo el primero producirian repartos afirmados con confianza y falsos, que es
peor que no repartir.

---

## Regla de continuidad

`tasksv2.md` es la fuente unica de verdad del estado de estas filas. Se actualiza **antes** de
tocar codigo y al terminar cada avance; despues, en el mismo cambio logico, se sincroniza
`Listav2/Listav2.js`. Los dos archivos suben juntos.

**El push a `main` despliega la web.** Cloudflare Pages esta conectado al repositorio por
integracion de git, no por un job de `.github/workflows/`, asi que no hay un paso de despliegue que
alguien pueda olvidar: cada push a `main` crea un despliegue de Production y el alias
`bptjersey.pages.dev` pasa a servirlo. Tres consecuencias que conviene tener presentes. Una fila de
frontend queda **desplegada** en cuanto su commit llega a `main`, no cuando alguien lo decida
despues. Un commit a medias en `main` sale a produccion igual, asi que `pnpm verify:mvp` antes de
empujar no es burocracia. Y las funciones **no** viajan en ese push: se despliegan aparte, por
`firebase deploy --only functions:<nombre>`, con la via de `functions.configDir` que describen
T025V2 y T022V2.

Cuando el cambio afecte a que ficheros escribe una fila, se actualiza tambien su entrada en
`TASK_SURFACES` y se regenera el bloque de reparto con `node Listav2/parallel-report.mjs`. La
prueba `listav2-ledger-sync` falla si el bloque se queda viejo, asi que no depende de acordarse.

---

## Grafo de conocimiento refrescado - 2026-09-10 (septima vuelta, y la deriva de ids cortada de raiz)

`graphify-out/` esta en `.gitignore`, asi que el grafo no viaja en el repositorio: lo que viaja es
esta nota.

- **6 ficheros reextraidos**, 5 de codigo y `tasksv2.md`. El grafo queda en **10.604 nodos** y
  **23.612 aristas**, en **519 comunidades**. Coste: 138.926 tokens de entrada en un solo
  subagente, 19 vueltas acumuladas y 3.888.282 tokens de entrada en total.
- Diferencia contra la vuelta anterior: **5 nodos nuevos, 88 aristas nuevas, 55 retiradas**. Las
  retiradas son el reemplazo normal de los ficheros reextraidos, no una perdida: el neto de aristas
  sube en 33.
- Los cinco nodos nuevos son `D9`, `T026V2`, el bloque de identidad de `AdminShell`, la dependencia
  de una sola cuenta administrativa que D9 deja declarada, y -con algo de ironia- la propia deriva
  de ids que esta nota describia la vuelta pasada.
- Diagnostico de integridad: **limpio**. Cero aristas colgantes, cero extremos ausentes, cero
  bucles, cero colapsos y cero duplicados exactos. Cero rutas absolutas en el grafo final.
- Las 519 comunidades **heredaron todas su nombre por contenido**; ninguna era nueva.

**Lo que cambia de verdad esta vuelta: el fallo de la deriva de ids se corrigio en el origen, no
despues.** La vuelta anterior lo dejo escrito -los ids de los nodos de concepto derivan de una
etiqueta que el modelo elige, y esa etiqueta no es estable entre vueltas, asi que un concepto
renombrado desaparece con toda su historia de aristas y reaparece sin ninguna-, y dejo escrito
tambien cual era el arreglo duradero: **pasarle al subagente la lista de ids que ya existen para ese
fichero**. Eso es lo que se hizo. Al subagente se le dio la lista de los **86 ids** que el grafo ya
tenia para `tasksv2.md`, con la regla explicita de que **la etiqueta puede cambiar y el id no**, y
la advertencia de que el documento solo habia crecido, asi que no omitiera nada por no haberlo
mencionado en esa lectura.

El resultado, comprobado y no supuesto: **86 de 86 ids conservados, cero desaparecidos, 5 nuevos**,
todos de material que de verdad es nuevo. Las vueltas anteriores necesitaron consolidar ocho pares
de ids y restaurar once conceptos con sus 27 aristas **despues** de la fusion; esta vuelta no hubo
nada que reparar. El guarda de encogimiento de `to_json`, que la vuelta pasada fue lo unico que
delato el problema, no tuvo que saltar.

**De los tres pasos manuales, uno queda resuelto por construccion y los otros dos siguen ahi:**

1. **Normalizar rutas a relativas antes de fusionar: sigue haciendo falta.** 264 esta vuelta, mas
   que las 151 de la anterior porque se toco mas fichero. Sin este paso las rutas absolutas entran
   en el grafo.
2. **Reimponer las etiquetas recien extraidas: esta vuelta fue un no-op, y conviene entender por
   que.** Solo **una** etiqueta difirio tras la fusion, y era exactamente el caso que la vuelta
   pasada documento: el extractor AST proponia `page.test.tsx` pelado donde el grafo ya tenia
   `admin/page.test.tsx`. Gano el grafo. La regla sigue siendo la misma -la extraccion mas reciente
   gana cuando describe un estado que cambio, no cuando es la etiqueta vieja recortada- y sigue
   siendo necesaria, aunque hoy no haya tenido trabajo que hacer.
3. **Consolidar la deriva de ids: resuelto en el origen.** Lo que habria que automatizar ya no es la
   reparacion, es el paso previo: extraer del grafo los ids del fichero que se va a reextraer y
   metérselos al subagente en el prompt. Son diez lineas de Python y quitan la parte fragil del
   proceso.

**Lo duradero sigue sin estar hecho:** un guion en `Listav2/` o en `scripts/` que envuelva el
refresco y haga esos tres pasos solo. Mientras no exista, cada vuelta depende de que alguien lea
esta nota.

**Una cifra que baja y no es una perdida:** las comunidades pasan de 529 a 519 aunque el grafo
crecio en 5 nodos. El agrupamiento se recalcula entero en cada vuelta sobre un grafo que cambio, y
a esa granularidad no es estable; ningun nodo se perdio -el recuento total lo confirma- y las 519
heredaron nombre. Conviene no leer ese numero como si midiera contenido.

**Comprobado nodo a nodo al terminar**, no supuesto: `T001V2`, `T012V2`, `T022V2` y `T025V2`
figuran como `(desplegada)`, `T026V2` como `(revision)`, `T021V2` como `(bloqueada)` y `T002V2`
como `(pendiente)`. `D8` y `D9` estan los dos, con su texto de hoy.

Aun asi, **para el estado de una fila este fichero sigue mandando**. No por desconfianza en las
etiquetas, que hoy estan bien, sino porque el grafo se refresca a mano y entre dos refrescos siempre
va por detras.

**Como se refresca:** no hay un solo comando. Es la skill `graphify` en modo incremental
-deteccion, extraccion AST, un subagente semantico por lote **al que se le pasan los ids ya
existentes**, **normalizacion de rutas a relativas**, fusion con `build_merge` podando los
reextraidos, **reimposicion de las etiquetas recien extraidas salvo la recortada**, traspaso de
nombres de comunidad por contenido y `graphify export html`-, y el interprete que usa esta fijado en
`graphify-out/.graphify_python`. Los pasos en negrita son los que la herramienta no hace sola.

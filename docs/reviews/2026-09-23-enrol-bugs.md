# Bugs de /enrol encontrados con Playwright (2026-09-23)

Plan `docs/superpowers/plans/2026-09-23-simple-access-recovery.md`, Task 1. Cada fila: síntoma, paso,
causa y arreglo (con el test que falló antes).

| # | Síntoma | Paso | Causa | Arreglo |
|---|---|---|---|---|
| E1 | Tras la aprobación del owner, el miembro con la sesión abierta ve "This area is for academy students" y no puede reservar (hasta ~1 h o cerrar sesión). | `/account` después de aprobar | `client-auth.tsx` confiaba en el rol `shopper` del token en caché; solo renovaba el token si la cuenta no tenía rol. | Renovar el token una vez si dice `shopper` (`client-auth.test.tsx` "opens the student area for a buyer the academy has just enrolled…", RED→GREEN). |
| E2 | Un adulto con solicitud abierta ve "Verify your email for family enrolment" (texto de tutores). | `/enrol` tras enviar | Condición `!applicantIsStudent \|\| openRequest`. | Se elimina con D8 (Task 2). |
| E3 | El owner no puede aprobar si la cuenta no verificó el email (y el tutor ni siquiera puede enviar: "Verify your email before sending a family enrolment request"). | `/enrol` pago (tutor) y `/admin/members/requests` | Puertas `emailVerified` en web y servidor. | D8: fuera las puertas; la aprobación marca la cuenta como verificada (`enrolment-approval-service.test.ts` "enrols an applicant who never verified…", `requests/page.test.tsx` "approves an applicant whose account email was never verified", RED→GREEN). El email distinto ya mostraba su motivo: no era bug. |
| E4 (solo emulador) | "Unable to send your request" al enviar con justificante. | `/enrol` pago | El almacén privado del emulador vivía en memoria y cada función corre en otro proceso. | Almacén del emulador en disco (`r2-client.test.ts` "shares emulator objects…", RED→GREEN). Producción usa R2. |
| E5 | Cada carga de `/account` lanza `getCourseCalendar` con `{warm:true}` y el servidor responde INVALID_ARGUMENT. | `/account` | Precalentado intencional de 63ee24d: la consulta vacía se rechaza a propósito tras comprobar la sesión (comentado en `firebase-calendar-repository.ts`). | No es bug: el contenedor arranca igual. Sin cambio. |
| E6 | El tutor en `/account` llama a `listNoShowPenalties` y recibe PERMISSION_DENIED ("Office access required"). | `/account` tutor | La vista de miembro pide las penalizaciones y captura el rechazo a propósito (`.catch(() => [])`, comentado). | No es bug: una llamada de más, sin efecto visible. Sin cambio. |
| E7 | Un miembro de prueba (trial) no puede reservar la Introduction Class: "ineligible". | `/account` trial → Book free intro | La reserva de intro exigía el waiver antiguo (`consents`), que el registro online ya no crea. | Se acepta también el waiver del registro (`intro-booking-service.test.ts` "books an intro for a trial member who accepted the enrolment waiver…", RED→GREEN). |
| E8 | El owner aprueba a un tutor y falla con `family_write_failed`. | `/admin/members/requests` Approve (tutor) | El escritor de familias exige tutor con email verificado; con D8 nadie verifica. | La aprobación marca la cuenta como verificada antes de escribir la familia (`enrolment-approval-service.test.ts` "vouches for an unverified tutor…", RED→GREEN). |
| E9 | Reservar fallaba a veces con "Booking operation failed" (INTERNAL). | `/account` Book | El nuevo control del waiver (D12) llamaba a `transaction.get` separado de su objeto y Firestore perdía `this`. | Llamada enlazada (`enrolment-waiver-acceptance.test.ts` "reads through a real transaction object…", RED→GREEN). |
| E10 | Los fallos desconocidos de reserva no dejaban rastro en el log. | servidor | `mapBookingError` convertía cualquier excepción en INTERNAL sin registrarla. | `logger.error("booking-operation-failed", …)` (así se encontró E9). |

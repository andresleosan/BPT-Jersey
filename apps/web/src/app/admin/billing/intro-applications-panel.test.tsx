import { cleanup, fireEvent, render, screen, waitFor } from "@testing-library/react";
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";
const api=vi.hoisted(()=>({list:vi.fn(),url:vi.fn(),review:vi.fn()}));vi.mock("../../../lib/intro-conversion-client",()=>({listIntroMembershipApplications:api.list,getIntroMembershipProofUrl:api.url,reviewIntroMembershipApplication:api.review}));
import { IntroApplicationsPanel } from "./intro-applications-panel";
const application={applicationId:"intro-application-1",requestId:"00112233-4455-6677-8899-aabbccddeeff",academyId:"academy-1",applicantUid:"user-1",studentId:"student-1",conversionId:"intro-student-1",site:"Town",planId:"town-adult",planName:"Town Adult",priceMinor:8500,currency:"GBP",billingPeriod:"monthly",planUpdatedAt:"2026-09-20T00:00:00.000Z",proofId:"a".repeat(64),bankReference:"INTRO-33",status:"pending_review",revision:0,decisionReason:null,approvedMembershipId:null,createdAt:"2026-09-21T12:00:00.000Z",updatedAt:"2026-09-21T12:00:00.000Z",schemaVersion:"1"};
describe("IntroApplicationsPanel",()=>{beforeEach(()=>{api.list.mockResolvedValue([application]);api.url.mockResolvedValue({url:"https://evidence.example/one",expiresAt:"2026-09-21T12:01:00.000Z"});api.review.mockResolvedValue({status:"approved"});vi.spyOn(window,"confirm").mockReturnValue(true);});afterEach(()=>{cleanup();vi.restoreAllMocks();vi.clearAllMocks();});it("shows pending plan, venue, amount and private evidence action",async()=>{render(<IntroApplicationsPanel/>);expect(await screen.findByText("Town Adult")).toBeVisible();expect(screen.getByText(/Town · £85.00 · INTRO-33/)).toBeVisible();fireEvent.click(screen.getByRole("button",{name:"View evidence"}));expect(await screen.findByRole("link",{name:"Open payment evidence"})).toHaveAttribute("href","https://evidence.example/one");});it("requires explicit confirmation before canonical approval",async()=>{api.list.mockResolvedValueOnce([application]).mockResolvedValueOnce([]);render(<IntroApplicationsPanel/>);fireEvent.click(await screen.findByRole("button",{name:"Approve"}));await waitFor(()=>expect(api.review).toHaveBeenCalledWith(expect.objectContaining({applicationId:application.applicationId,decision:"approve",expectedRevision:0})));expect(window.confirm).toHaveBeenCalled();});});
